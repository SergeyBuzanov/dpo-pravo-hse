# Прогон сайта ДПО по новым скиллам – 03.09.2026

Общий список находок. Порядок прогона: web-design-guidelines (Vercel) →
скиллы Османи (frontend-ui-engineering, performance-optimization,
security-and-hardening, observability, code-review-and-quality) →
сверка apple-design на регрессии → сравнение DESIGN.md с эталонами
awesome-design-md → контрольная impeccable critique.

Приоритеты: P1 – ломает доступность или сценарий пользователя;
P2 – нарушение правила с видимым эффектом; P3 – гигиена.
Правится только по решениям заказчика. Исходник лендинга –
`.landing-template.html` (template:extract → правка → template:inject).

Состояние на старте: коммит `98bce29`, apple-design закрыт полностью,
последняя impeccable critique – 25/36.

## 1. web-design-guidelines (Vercel)

Правила взяты с raw.githubusercontent.com/vercel-labs/web-interface-guidelines
03.09. Проверено пятью параллельными ревью: `.landing-template.html`
(две половины), js/ формы и модалки, js/ поведение, внутренние страницы
(privacy, ratings, 404, каталог, страница программы + генератор, admin).
Русская типографика («», «–») правилом не считается нарушением; Title Case
и английские правила копирайта не применялись. Две находки перепроверены
вручную (отмечены ✔).

Итог: 2 P1 (одна публичная, одна в админке), 17 P2, ~55 P3.
Всего сквозных тем шесть – они вынесены отдельно, чтобы не дублировать.

### P1

1. ✔ **Форма заявки теряет «Комментарий».** `js/application-form.js:806`
   – `collect()` не читает поле `comment`, хотя textarea есть (:463),
   сервер его ждёт (`lib/application-form.js:194`) и вкладывает в письмо
   (`lib/application-delivery.js:105`). Подсказки тем (:61–65) просят
   писать «Идею курса», «Стать преподавателем» и «Отзыв» именно в
   комментарий – три темы из четырёх приходят пустыми. Не правило WIG,
   а функциональный дефект, найден по ходу проверки формы.
2. **[admin]** `admin.html:788` – поля редактора каталога (тип :809,
   название :812, формат :833, цена :836, старт :839, длительность :842,
   url :845) без `<label>`/`aria-label`, ~26×7 неподписанных полей;
   чекбокс блокировки (:848–851) назван только через `title`.

### P2 – публичные страницы

3. **Лента стартов крутится без кнопки «стоп».** `.landing-template.html:2441`
   `#startsTrack` (`data-dpo-loop data-dpo-speed="12"`), `js/carousel.js:118`,
   генератор `scripts/build-landing.js:1021–1024`. Пауза только по
   наведению/фокусу; у `#reviewsTrack` кнопка есть (:4046). WCAG 2.2.2.
   Это решение заказчика 20.08 («листать просто мышкой») – нужно либо
   добавить такую же кнопку, либо зафиксировать отступление в DESIGN.md.
   РЕШЕНИЕ ВЛАДЕЛЬЦА.
4. ✔ **`style-hover` мёртв на 11 элементах.** `.landing-template.html:2411`
   (герой «Подать заявку»), :2412 («Смотреть программы»), :2824,
   :3106 («Пройти опрос»), :4384 и :4511 (`.dpo-cta-gold`), :4422
   (`.explore-card`), :4489 («Подписаться»). Проверено: рантайм
   `js/bundle/dc-runtime.js` не содержит ни одного «hover», атрибут
   ничем не обрабатывается – у восьми CTA нет hover-состояния.
5. **Подсказки формат/документ недоступны с клавиатуры.**
   `.landing-template.html:1621` (CSS `::after content: attr(data-tip)`
   только на `:hover`), разметка :3070–3071. `<span>` не фокусируется,
   Esc не закрывает. WCAG 1.4.13.
6. **Нет `scroll-padding-top` при липкой шапке – везде.**
   `.landing-template.html:584` (шапка 110/64px, якоря `#formats` :2266,
   `#top` :2246, скип-ссылка :12), `privacy.html:54`, `ratings.html:53`,
   `Каталог программ.html:95` (плюс второй липкий ряд фильтров :269),
   `scripts/build-program-pages.js:1012`. Цель якоря и Shift+Tab-фокус
   уходят под шапку; в каталоге `scroll-margin-top` есть только у `#filters`.
7. **Телефон в контактах не ссылка.** `.landing-template.html:4496` –
   «+7 (495) 772-95-90, доб. 22112» в `<div>`, нужен `<a href="tel:…">`.
8. **Фон героя (LCP) без preload.** `.landing-template.html:2397` –
   `background-image: image-set(…)` на `<div>`, `<link rel="preload" as="image">`
   в `<head>` нет, `fetchpriority` на фоне невозможен.
9. **Шапка анимирует layout-свойства при прокрутке.**
   `.landing-template.html:591` (`height`, `padding`, `background`), :609
   (`gap`, `padding`), :635 (`height`, `padding`, `font-size`), :1164, :1210.
   Под reduced-motion выключено (:1378), но на каждом пересечении 120px
   – перелайаут. Переводить на transform/opacity.
10. **`scroll-behavior: smooth` без reduced-motion.**
    `.landing-template.html:1440` `.dpo-top5-track`; блок :2205 сбрасывает
    только `.dpo-track`.
11. **Reveal через `filter: blur`.** `js/smooth-ui.js:100–104` – `.dpo-reveal`
    на каждой секции/карточке анимирует blur, перерисовка целых секций;
    карточки (:120) уже без filter.
12. **Мобильное меню отключает пинч-зум.** `js/nav-menu.js:131` –
    `panel.style.touchAction = 'none'` на всей панели; достаточно `pinch-zoom`.
13. **Опросник не запирает страницу.** `js/quiz.js:60` – фон `overflow-y:auto`
    без `overscroll-behavior:contain`, `open()` (:178–202) не блокирует
    `documentElement.overflow` и не ставит `inert` (форма заявки :695–735 и
    team-modal :209 это делают).
14. **Escape сбрасывает заполненную заявку.** `js/application-form.js:538`
    (и клик по фону :528–531) – диалог удаляется через 220 мс (:749–755),
    повторное открытие даёт пустую форму. Нужно подтверждение или
    сохранение черновика.
15. **Невалидный `font:` в приглашении в канал.** `js/channel-invite.js:64`
    `font:600 0.9375rem/1.35 inherit` – `inherit` внутри шортката,
    Chrome отбрасывает всё объявление; `.ci-title` теряет вес и размер
    на десктопе (мобильный блок :88 возвращает только размер).
16. **Сброс cookies – ссылка вместо кнопки.** `privacy.html:122`
    `<a href="#" onclick="window.resetCookieConsent()">` → `<button type="button">`.
17. **Фильтры каталога не пишутся в URL.** `Каталог программ.html:1626`
    (:1632, :1640, :1646) – состояние читается из `?type/format/sphere/duration/q/sort`
    (:1678–1706), но `history.replaceState` нет: отфильтрованный вид нельзя
    переслать, «Назад» его не восстанавливает.
18. **Нижние панели без safe-area.** `scripts/build-program-pages.js:1171`
    `.buy-bar{bottom:12px}` + `body{padding-bottom:84px}` (:1179,
    programs/program.css:196/204); то же у мобильной CTA лендинга
    `js/smooth-ui.js:190/213`. Каталог делает правильно (:359/380).
    Viewport везде без `viewport-fit=cover`, так что сейчас инсеты = 0;
    чинить вместе с meta.

### P2 – админка

19. **[admin]** `admin.html:764` – `editorDirty` считается, точка показана,
    но `beforeunload` нет: закрытие вкладки молча теряет правки (confirm
    есть только у refresh :967 и reload :1000).

### Сквозные P3 (по всему сайту)

- **`touch-action: manipulation` и `-webkit-tap-highlight-color` не заданы
  нигде** – 300 мс задержка двойного тапа на iOS и серая вспышка на Android
  у всех кнопок. Естественное место – глобальное правило
  `js/smooth-ui.js:31` (`a, button, [role="button"]`).
- **`env(safe-area-inset-*)` нигде**, viewport без `viewport-fit=cover`:
  шапка (`.landing-template.html:703`), полноэкранное меню (:2377),
  тёмные full-bleed секции (:2842, :3467, :4355, :4509), панели из п. 18.
- **`text-wrap: balance` нет на заголовках**: `.dpo-h2` (:1391), `<h1>`
  (:2401), inline `<h2>` (:2810, :2925, :3477, :4376, :4433),
  `privacy.html:70`, `ratings.html:69`, `404.html:96`, `Каталог программ.html:163`.
  Есть только у `.dpo-doc-display` (:1948).
- **Нет `<link rel="preload" as="font">`** для HSE Sans/Slab Regular:
  `.landing-template.html:8` (шрифты :49/:52), `privacy.html:30`,
  `ratings.html:29`, `Каталог программ.html:41`, `scripts/build-program-pages.js:821`.
  `font-display: swap` есть, поэтому FOUT, а не FOIT.
- **Элементы вне `.dpo-scope` не получают фирменного focus-ring**
  (остаётся UA-обводка): cookie-баннер `js/cookie-consent.js:91`,
  приглашение в канал `js/channel-invite.js:67/72/112`, мобильная CTA
  `js/smooth-ui.js:198` (у неё и hover нет).
- **`overscroll-behavior: contain` нет у фонов модалок**:
  `js/application-form.js:94`, `js/team-modal.js:24`, `js/quiz.js:60` (п. 13).

### P3 – лендинг (`.landing-template.html`)

- :1696 `.dpo-scope button:focus-visible { border-radius: 4px }` бьёт
  пилюли 999px (`.dpo-nav-cta`, `.dpo-tile-apply`, `.dpo-doc-tab`,
  `.dpo-menu-all`, `.dpo-carousel-btn`) – при фокусе углы «квадратятся».
  Лечится `border-radius: inherit`.
- :1994 + `js/document-switch.js:59` – `<div role="tabpanel">` (:3958)
  с `cursor: pointer` и кликом, листающим документы; нет клавиатурного
  эквивалента именно для этого действия (табы остаются).
- :1356/:1358 `.dpo-menu-all` и :1982/:1984 `.dpo-doc-tab` – transition +
  `scale(0.97)` без сброса под reduced-motion (остальные сброшены :1364–1367, :1670).
- :1256 `.dpo-menu-panel { max-height: calc(100vh - …) }` – на iOS Safari
  низ меню («Подать заявку») под тулбаром; нужен `100dvh`.
- :7 `theme-color #1658DA` при фоне страницы `#FBF9F5` (:516) и шапки
  `#FAF7F1` (:576) – обвязка браузера не совпадает со страницей.
- :2289 «ещё 3 программы» / «6 программ» (:2282, :2306, :2313) – число и
  слово разделены обычным пробелом, генератор `scripts/build-nav-panel.js`.
- :2248 лого «Право» / «НИУ ВШЭ» без `translate="no"`.
- :1583 `.dpo-tile-start:empty` не срабатывает: рантайм оборачивает
  `{{ p.start }}` (:3061) в `<span class="sc-interp">`; обход через `:has`
  в `js/smooth-ui.js:222` есть только для `.dpo-tag`. ПРОВЕРИТЬ В БРАУЗЕРЕ.
- :2441 и :4049 `aria-label` на `<div>` без роли – не читается.
- :2377 `role="group" aria-label="Меню"` оборачивает `<nav aria-label="Меню">` – имя дважды.
- :4453, :4497 `<a href="#" class="email-protect">` – без JS уводит наверх.
- :3531–3849 портреты преподавателей без `width`/`height` (CLS держит
  `position:absolute; inset:0`); генератор `scripts/build-landing.js`.
- :4108, :4116, :4172 (+ дубли :4253, :4261) « - » дефис как тире в
  цитатах отзывов; отзывы verbatim по замыслу (комментарий :4020–4024).
- :4422 `.explore-card` transition `border-color`, :3162 `.dpo-prog-apply`
  transition background/border/color – не compositor-свойства.

### P3 – js/

- `application-form.js:416/417/465` плейсхолдеры с примером без «…»;
  :432/:433 телефон и почта без образца формата.
- `cookie-consent.js:87` `font-family` дважды в одном правиле.
- `channel-invite.js:152/195` `<img>` без `width`/`height`; :304 `place()`
  читает layout (:279 `offsetHeight`, :280 `getBoundingClientRect`) каждые
  500 мс всё время жизни карточки, resize-слушатель (:311) не снимается
  при закрытии (:248–253).
- `team-modal.js:208` фон не `inert`/`aria-hidden` (форма заявки делает :723–735).
- `smooth-ui.js:146/149` transition `box-shadow` + постоянный
  `will-change: transform, box-shadow` на всех explore-картах;
  :14 reduced-motion читается один раз без `change`-слушателя (:18, :251);
  :422–424 в failsafe-цикле чередуются `getBoundingClientRect` и
  `classList.add` – forced layout на каждой итерации каждые 700 мс.
- `nav-menu.js:150/222` и `sheet-gesture.js:76/150` – `offsetHeight` на
  каждом pointermove и кадре пружины; кэшировать в pointerdown.
- `carousel.js:159–190` rAF-цикл крутится вечно на всех страницах и
  каждый кадр ищет `[data-dpo-loop]`, даже где лент нет.
- `sheet-gesture.js:41/64` `touch-action: none` на ручке и заголовке
  диалога – блокирует пинч-зум с шапки; хватит `pinch-zoom`.
- `document-switch.js:21` выбранный документ не отражён в URL.
- `site-analytics.js` – ✓ чисто.

### P3 – внутренние страницы

- `ratings.html:133–145` английские ячейки без `lang="en"`; бренды
  QS/THE/RAEX/hh.ru/SuperJob/Forbes (:132, :140, :147, :159, :164, :172)
  без `translate="no"`.
- `update-catalog.js:177` миниатюры карточек без `width`/`height`
  (26 карточек, CLS держит `.card-media{height:150px}`).
- `Каталог программ.html:578` цены карточек без `tabular-nums`
  (`.flag-date` :227 имеет); :937 поиск без `name`; :898/:906 ряды
  фильтров без имени, :914/:925 `aria-label` на `div` без `role="group"`;
  :429–434 transition `box-shadow`/`filter`, blur при смене фильтра
  (:456, :472) перерисовывает 26 карточек; :662 `.card-link:focus-visible{outline:none}`
  – кольцо живёт на `.card:has(...)` (:658), без `:has` фокуса не видно.
- `lib/hse-catalog.js:246` `formatPrice` ставит обычный пробел перед «₽» –
  «50 000 / ₽» переносится в `.price` страницы программы
  (program.css:168 без nowrap) и в карточках; нужен nbsp.
- `scripts/build-program-pages.js:768` hero `<img>` первого экрана без
  `fetchpriority="high"`; :1152/:1154/:1036/:1137/:1163 transitions и
  `scale(.97)` без `prefers-reduced-motion` (гейт только у scroll-behavior :997).
- `404.html` – ✓ чисто, кроме `text-wrap: balance`.

### P3 – админка [admin]

- `admin.html:565` `<label>Период:</label>` без контрола; :506–508 чекбокс
  и подпись соседи с зазором (мёртвая зона); :809/:833/:836/:842
  плейсхолдеры без «…»; :572 `#analyticsMeta` и :659 `#applicationsMeta`
  получают async-статусы без `aria-live`; :1189–1192 столбики часов
  несут значение только в `title`; :1205 `d.day.slice(5)` → `Intl.DateTimeFormat`;
  :490/:500/:526/:670 заголовки секций – `<p>`, у вкладки каталога нет `h2`;
  :203/:221 KPI без `tabular-nums`; :95/:98/:183/:190 transitions без
  reduced-motion; :6 нет `theme-color`.

### Что проверено и чисто

Viewport без `user-scalable=no`; скип-ссылки на всех страницах и цели
существуют; иерархия заголовков; `:focus-visible` с заменой везде, где
`outline:none` (кроме вне-scope элементов выше); hover-состояния под
`(hover:hover) and (pointer:fine)`; `prefers-reduced-motion` в меню,
шапке, треках, форме, опроснике, модалках (кроме пп. 10, 11 и
перечисленных P3); ни одного `transition: all`; фокус-ловушки и Esc во
всех трёх диалогах, возврат фокуса открывшему; все иконочные кнопки с
`aria-label`; табы документов – полноценный `tablist`; сканы документов с
размерами, `loading="lazy"`, WebP; все 35 цен лендинга с `&nbsp;` перед
«₽»; `tabular-nums` на счётчиках; `Intl` в `formatPrice`/`formatDate`;
поля формы заявки с `label`, `autocomplete`, `type=tel/email`, inline-ошибки
с `aria-describedby`, фокус на первую ошибку, `role=status`; кнопка отправки
блокируется только на время запроса с «Отправляем…»; honeypot скрыт от AT;
внешние ссылки `rel="noopener noreferrer"`; `color-scheme` не нужен
(единственная светлая тема).

## 2. Линзы Османи – ВЫПОЛНЕНО (пять линз, после перезапуска)

Первый запуск пяти субагентов сгорел на лимите сессии; замеры Lighthouse,
тесты и gzip-бюджет сняты вручную, затем все пять линз перезапущены и
дошли до конца. Сырые отчёты `osmani-*.md` – в scratchpad сессии (пропадут).

### 2.0 Сводка по приоритетам

**Critical**
- C1 [security] Зеркало отдаёт весь main. ✔ Проверено: 200 на
  `admin.html`, `admin-server.js`, `lib/smtp.js`, `.catalog-data.json` и
  `docs/superpowers/plans/2026-07-24-dpo-site-fixes.md` (ФИО сотрудницы и
  добавочные номера); `<meta name="robots" content="index, follow">`.
  Ветка `mirror/gh-pages` с чистой выкладкой существует (scripts/build-public.js),
  источник Pages не переключён. Пункт №2 аудита 18.08, решение владельца
  25.08 не исполнено. Действие: Settings → Pages → gh-pages / (root);
  убрать файл плана из main (или вычистить ФИО); до переключения – noindex.

**P1**
- [perf] Лендинг на телефоне: Perf 51, LCP 6,2 с, TBT 1,3 с на зеркале
  («Poor»). Причина – рендер в браузере React-рантаймом сборщика (§2.1).
- [observability] Исход письма не сохраняется; владелец не видит
  недоставленных заявок (§2.5).
- [quality → P1 Vercel] Нет теста-контракта клиент ↔ сервер формы, из-за
  чего и потерян «Комментарий» (§2.2).

**P2 / Required** – 7 security (§2.3), 5 frontend (§2.4), 3 perf (§2.1),
4 quality (§2.2), 5 observability (§2.5). Ниже по линзам.

### 2.1 performance-optimization – ЗАМЕРЕНО

Lighthouse (`npx lighthouse`, headless Chrome, mobile = эмуляция медленного
4G и слабого CPU). Локальный сервер без gzip, поэтому отдельно снято
публичное зеркало (gzip есть: документ 93 КБ вместо 492).

| Страница | Perf | FCP | LCP | TBT | CLS | Байт |
|---|---|---|---|---|---|---|
| Лендинг mobile, локально (медиана из 3) | 40 (39–41) | 3,63 с | 8,69 с | 1875 мс | 0,000 | 1482 КБ / 25 |
| **Лендинг mobile, зеркало (2 прогона)** | **51** | 1,3 с | **6,2 с** | **1290–1390 мс** | 0,006 | 818 КБ / 25 |
| Лендинг desktop, локально | 83 | 0,75 с | 1,58 с | 264 мс | 0,000 | 1146 КБ |
| Каталог mobile, локально | 90 | 2,29 с | 3,24 с | 46 мс | 0,051 | 486 КБ |
| Страница программы mobile | 92 | 1,68 с | 3,17 с | 43 мс | 0,018 | 364 КБ |

Accessibility / Best practices / SEO = 100 везде; каталог – a11y
`label-content-name-mismatch` на 26 кнопках `.card-apply` (§2.4).

- **P1 – лендинг рендерится в браузере.** Long tasks:
  `js/vendor/react-dom-18.3.1.production.min.js` 465–655 мс, `react` 343–500,
  `smooth-ui.js` ~400, `team-modal.js` ~300. Страница собирается рантаймом
  визуального сборщика (`js/bundle/dc-runtime.js` + React + ReactDOM, 142 КБ
  raw) из `<script type="__bundler/template">`; до отработки React контент
  с сервера не приходит. Зеркало отдаёт ту же сборку (проверено). Каталог и
  страницы программ статичные – 90 и 92, что подтверждает диагноз. Лечение:
  пререндер лендинга в статичный HTML на сборке (`scripts/build-public.js`
  уже собирает `.public/`), рантайм оставить только если он нужен после
  гидратации. Desktop не страдает из-за CPU в 4 раза быстрее.
- **P2 – телефону уходит более тяжёлая картинка, чем десктопу:** Image
  510–515 КБ / 3 на mobile против 179 КБ / 2 на desktop. `image-set` героя
  (`.landing-template.html:2397`) даёт 2x-вариант `images/hero-composite.jpg`
  (343 КБ) экранам с DPR ≥ 2, 1x `hero-composite-1400.jpg` (174 КБ) – десктопу.
  `image-delivery-insight`: экономия 200 КБ. Нужен мобильный кроп и WebP/AVIF.
- **P2 – JS не минифицирован:** `unminified-javascript` 97 КБ, `unused-javascript`
  79 КБ; 14 script-запросов. gzip: application-form 16 КБ, dc-runtime 17,
  smooth-ui 11, nav-menu 6, channel-invite 6, остальные 2–4.
- P3 – `render-blocking-insight` 440–590 мс на всех страницах; кеш: локально
  `no-cache`, зеркало `max-age=600`, прод-nginx год на шрифты/картинки
  (docker/nginx.conf:44), js/*.js без хэшей в именах кешировать надолго нельзя.

Бюджет (gzip) против целей скилла: JS 128 КБ < 200 ✓; CSS ~2 КБ ✓; шрифты
86–87 КБ / 5 файлов < 100 ✓ (HSE Sans Regular/SemiBold/Bold + HSE Slab
Regular/Black; IBM Plex по unicode-range в первый экран не попадает);
документ лендинга 92 КБ gz / 492 raw – в 5 раз больше каталога (29 КБ gz);
картинка первого экрана на телефоне 343 КБ > 200 ✗.

Пункты Vercel (preload шрифтов, hero без preload, blur reveal, шапка с
layout-анимацией, вечный rAF) на фоне P1 второстепенны: LCP задаёт React.

### 2.2 code-review-and-quality

- **ОТМЕНА: `npm test` НЕ виснет.** Мой прогон молчал 9 минут под нагрузкой
  Lighthouse + Playwright, а первый запуск вовсе не стартовал (на macOS нет
  `timeout`). Линза прогнала параллельно 4 раза: 160/160 за 21,7–22,1 с,
  `npm test` тоже. Сокеты smtp.test.js закрываются (:22–27, :86–89),
  fetch-timeout.test.js – `finally { server.close() }`, порты только `listen(0)`.
  Совет из памяти `env -u NODE_OPTIONS` остаётся в силе. `template:verify` ✓.
- **Required – нет теста-контракта клиент ↔ сервер формы.** `js/application-form.js:806–833`
  `collect()` шлёт 15 ключей + program*, `lib/application-form.js` читает 18
  `raw.*`: разница – `website` (ловушка, читается раньше, admin-server.js:237)
  и **`comment`** (:194), который клиент не шлёт. `tests/unit/application-form.test.js:8–14`
  кормит `parseApplication` ручными объектами – дрейф невидим. Лечение:
  `tests/unit/application-form-contract.test.js` по образцу
  `application-purge-wired.test.js` (регэксп по исходникам обеих сторон,
  равенство множеств с одним исключением `website`). Падает сегодня, пройдёт после фикса.
- **Required – модули без тестов на недоверенных границах:**
  `lib/program-slug.js` (26 URL программ, sitemap), `lib/analytics-store.js`
  (`sanitizeEvent` – единственный парсер `POST /api/collect`),
  `lib/admin-credentials.js` (только через живой сервер в security_test.py),
  `scripts/fetch-program-media.js` (экспорты «для тестов», тестов нет),
  `admin-server.js` (только black-box). Первые три файла тестов – дёшево.
- **Required – `scripts/static-server.js:253, :266`** `catch { /* приём заявок
  необязателен */ }` глотает ЛЮБУЮ ошибку `require` модуля чистки ПДн –
  синтаксическая ошибка молча выключит годовую чистку; тест purge-wired
  греп-ом остаётся зелёным. Лечение: `if (err.code !== 'MODULE_NOT_FOUND') throw err`.
- Consider – `lib/hse-catalog.js:15` `FETCH_TIMEOUT_MS = 20_000` без
  переопределения: `fetch-timeout.test.js` ждёт 20 с из 22; параметр
  `{ timeoutMs }` как в `lib/smtp.js:156` – сборка за ~2 с.
- Consider – `admin-server.js:684, :697, :734` `/* ignore */` – при битом
  `.catalog-data.json` админка молча показывает старые числа.
- Consider – дубли: escapeHtml ×4 (`scripts/build-landing.js:93`,
  `update-catalog.js:76`, `scripts/build-program-pages.js:113`,
  `js/application-form.js:882` – последний DOM, законно), enDash ×2,
  formatPrice ×2 (`build-landing.js:168` + `:910–913` своё «Бесплатно» vs
  `lib/hse-catalog.js:240` с тестом), writeAtomic: канон
  `lib/catalog-store.js:73`, копия `application-store.js:193`, не атомарно
  `admin-server.js:374` и `lib/admin-credentials.js:79/:100`. Лечение:
  `lib/html-text.js` (escapeHtml + enDash), импорт formatPrice из lib,
  `writeAtomic(file, content, { mode })` во всех четырёх.
- Consider – `|| ''` ниже границы, которую store уже гарантирует:
  `scripts/build-program-pages.js:95–96` `id: String(p.id || '')` в
  `content/program-index.json` – пустой id даст заявку без предмета.
- Nit – `.claude/launch.json` отслеживается, хотя `.gitignore` его закрывает;
  история: index.html 95,5 МиБ в 144 версиях (до unbundle по 2,78 МБ),
  pack 16,8 МиБ, 1937 loose-объектов (53 МиБ) – локальный `git gc`.
- Швы файлов > 1000 строк: build-landing.js – только вынос текстовых
  хелперов (:91–190) окупается; build-program-pages.js – вынести CSS-литерал
  (:976–1219, ~240 строк) в настоящий `programs/program.css`-исходник;
  admin-server.js – `lib/admin-guard.js` (:99–345) и
  `lib/admin-catalog-routes.js` (:490–673) при следующей фиче там.
- Мёртвого кода нет (ни одной функции без вызова); мёртвые только
  записи `module.exports` (≈25: `hashPassword`, `mailConfig`, `LIMITS`,
  `DATA_FILE`…, `renderPage`, `extractOgImage`…, `writeCatalogHtml`) –
  список в отчёте линзы; удалять по решению.
- TODO/FIXME – ноль. Пустые catch на клиенте – все про localStorage, с комментарием.

### 2.3 security-and-hardening

Модель угроз по семи границам (форма, маяк, админ-маршруты, рендер в
admin.html, SMTP, файлы, загрузка с hse.ru, деплой) – в отчёте линзы.
Основа крепкая: auth на каждом маршруте (`checkAuth` :1076 раньше таблицы
:970–986), CSRF на всех изменяющих, лимиты тела/URL/методов, `str()`
схлопывает CR/LF (инъекция в Subject/Reply-To невозможна), тема RFC 2047,
тело base64, escapeHtml на всех полях в admin.html, `Internal error`
наружу, traversal закрыт дважды, SSRF в основном загрузчике закрыт
(сверка `res.url`), секретов в истории нет, `.applications` 0700/0600,
аналитика без IP/UA и fail-closed по согласию, вход: lockout + тарпит +
timingSafeEqual, порт админки только на 127.0.0.1, `USER node`,
`no-new-privileges`, sha256 обоих React-файлов совпадает с unpkg.

- **C1 – зеркало отдаёт весь main** (см. 2.0).
- **R1** `.dockerignore` + `Dockerfile:6` `COPY . .` – `.applications/` не
  исключён: пересборка на сервере запекает заявки с ФИО в слой образа
  (открыто с 18.08). Одна строка.
- **R2** `lib/admin-credentials.js:79, :100` – `.admin-credentials.json`
  создаётся 0644 (подтверждено на диске): логин + соль + scrypt-хеш
  читаемы любым локальным пользователем. `{ mode: 0o600 }`.
- **R3** нет `remove(id)` в `lib/application-store.js` и маршрута удаления –
  privacy.html §3 обещает уничтожить данные за 30 дней после отзыва, а путь
  – только правка JSONL 0600 внутри контейнера руками. Та же конструкция,
  что была с purge до 21.08.
- **R4** `docker/nginx.conf` без `access_log`/`log_format`, compose без
  `logging:` – nginx пишет IP + UA + URL каждого `POST /api/application` в
  `docker logs` без ротации, а privacy §2–3 утверждает «IP не сохраняются».
  `access_log off` на двух location или `log_format` без `$remote_addr`,
  плюс `max-size/max-file`; в политике упомянуть технические логи.
- **R5** `Dockerfile:3` `node:20-alpine` (EOL 30.04.2026), `nginx:1.27-alpine`
  – теги без digest, сборка невоспроизводима.
- **R6** `js/application-form.js:454` + `lib/application-form.js:195–198` –
  согласие на анонсы opt-out; ст. 18 ч. 1 38-ФЗ требует предварительного
  согласия. Копирует маркетплейс – РЕШЕНИЕ ВЛАДЕЛЬЦА/ЮРИСТОВ.
- **R7** `lib/application-form.js:178–204` – в записи нет `consentAt`,
  редакции политики и текста формулировки: доказать согласие через год
  нечем (152-ФЗ ст. 9 ч. 3).
- Consider (K1–K15, в отчёте линзы): STARTTLS не обязателен при пустом
  SMTP_USER (smtp.js:230–238); не-ASCII адрес заявителя попадает в текст
  ошибки и лог (smtp.js:139, delivery:158); `program.url` из тела запроса
  печатается в письмо менеджеру без проверки origin (delivery:106,
  application-form.js:202); `employeesCount`/`timeframe` не выводятся в
  admin.html (собираются, не используются); purge помесячно – заявка живёт
  до 13 мес.; `/api/collect` без `client_max_body_size`; captureConsole
  подменяет глобальный console (:378–409); fetch-скрипты не сверяют URL
  после редиректа; Basic auth без «выйти»; sha256 React-файлов нигде не
  зафиксирован (тест с зашитыми хешами); `.gitignore` без `.env*`; ответ
  ловушке без `id` выдаёт распознавание робота (:238 vs :254); privacy.html
  (16.08) отстала от кода 01.09 (корпоративные поля, три темы обращений);
  `.analytics/` 0755; `setStatus` принимает любой id (:774–796).
- Таблица ПДн (поле → куда попадает → срок → удаление) – в отчёте линзы;
  вывод: каждое поле живёт в трёх местах (журнал, письмо, admin) плюс слой
  образа (R1), срок фактически до 13 мес., путь удаления отсутствует (R3).
- Из аудита 18.08 закрыто: #3 срок хранения (проводка + тест), батарея
  security_test.py бьёт по admin-server. Открыто: #2 (C1), `.dockerignore`
  (R1), rsync без `--exclude .applications` (ci.yml:157–162), node:20 (R5),
  redirect в fetch-скриптах, captureConsole, не-ASCII Reply-To, удаление
  одной заявки (R3), consentAt (R7), STARTTLS (K1), холостая проверка 404
  в CI (ci.yml:132–134), согласие на анонсы (R6).

### 2.4 frontend-ui-engineering – В БРАУЗЕРЕ (playwright-cli, Chromium, axe 4.10.2)

Консоль: 0 ошибок и предупреждений на 5 страницах × 4 ширины. Фокус-кольцо
видно на всех 26 стопах лендинга; диалог заявки – фокус на заголовке,
ловушка, inert, Escape возвращает фокус. Иерархия заголовков чистая
везде, один H1, `main#main`. axe – ни одного serious/critical.
49 скриншотов в scratchpad `pw/` (пропадут).

- **P2 – каталог уезжает вбок на 320/768/1440.** `.card .tag[data-tip]::after`
  (`Каталог программ.html:529`) – подсказка `position:absolute; width:250px;
  right:-139…-237px` на каждой карточке выходит за сетку: `scrollWidth`
  460/834/1501 при `innerWidth` 320/768/1440, колесо даёт `scrollX` 140/66/61.
  Скрытие `.card` убирает overflow. На 1024 не проявилось.
- **P2 – каталог @ 320:** `.grid` `minmax(300px, 1fr)` (:416) при контейнере
  280 px – карточка упирается в правый край; `header a.back` (:124 nowrap)
  обрезан до «← Главн».
- **P2 – «Подать заявку» открывает диалог И уводит страницу к контактам.**
  `a.dpo-nav-cta[href="#contacts"][data-application]` (также CTA героя и
  мобильная CTA): `js/smooth-ui.js:255–281` в capture-фазе делает
  smoothScrollTo(#contacts), `focus()` и `pushState('#contacts')`, затем
  `application-form.js:958–963` открывает диалог. Замер: scrollY 0 → 10 849
  при открытом диалоге; после Escape пользователь внизу страницы, фокус на
  шапке. Потеря контекста + лишняя запись в истории.
- **P2 – каталог без единого `<nav>`;** axe `region` (moderate) – 9 узлов
  вне ориентиров: eyebrow, h1, три `p`, секция стартов лежат ДО `main#main`.
- **P2 (из Lighthouse)** – 26 кнопок `.card-apply`: `label-content-name-mismatch`
  (`update-catalog.js`, aria-label не содержит видимый текст).
- P3 – `.logo .sub` 10 px в каталоге и на странице программы (`:121`,
  `program.css:53`), `span.dpo-start-mon` 11 px, `.dpo-brand-sub` 9 px;
  privacy/ratings @ 320 – липкая шапка 136 px (четверть экрана 568);
  landing @ 320/768 – `nav.dpo-mobile-cta` (z 940) под cookie-баннером
  (z 1000), Tab попадает на невидимые ссылки; program @ 1440×768 – `aside.side`
  sticky top 96, низ 809 за краем (41 px недоступны); `#startsTrack` – 24
  Tab-стопа подряд без пропуска ленты; program – img/h1/chips героя до
  `main` (axe region ×3); landing – `aside` внутри `main`
  (`landmark-complementary-is-top-level`); `#starts h2` 22 px – единственный
  H2 не на ступени; catalog @ 768/1440 – 2–4 миниатюры `ERR_ABORTED`
  (запрос стартует и отменяется, файлы есть); пустое состояние каталога
  «По выбранному фильтру…» при поиске, без действия внутри блока
  (сброс живёт выше).
- Шкала отступов vs DESIGN.md (1440): 18 из 28 замеров на шкале. Вне:
  `#starts` 52/64, `#final-cta` 44/64, `.dpo-tile-apply` 0 18, `.dpo-sphere`
  24 24 0, `.dpo-teacher` 28 при card-lg 40 и radius 24, `li.dpo-format`
  28/0 gap 12 36 при «32px / 14px» в DESIGN.md, `.dpo-h2` отбивки
  22/0/18/20/12 при «14px 0 40px» – документ и код разошлись.
- Не проверялось: Safari/Firefox, реальные телефоны и safe-area, масштаб
  200 %, VoiceOver, axe на privacy/ratings, 404.html.

### 2.5 observability-and-instrumentation

Вопросы дежурного → что отвечает сегодня:
1. Доходят ли заявки до почты? – только `console.error` при отказе
   (lib/application-delivery.js:158) в `docker logs`. **P1.**
2. Падает ли SMTP? – та же строка; `/api/health` (:879–939) SMTP не
   проверяет. **P1.**
3. Сайт жив / 5xx? – access_log nginx по умолчанию; HEALTHCHECK бьёт в
   `/api/csrf` (Dockerfile:27–28); `/api/health` за Basic auth и ходит в
   hse.ru, из healthcheck не вызвать; на `unhealthy`/503 ничто не реагирует;
   внешнего пинга нет. P2.
4. Злоупотребляют ли маяком? – три рубежа (nginx 10 r/s, Node 600/мин,
   Origin), но отказы не логируются и `{accepted, rejected}` никто не читает. P3.

- **P1** `lib/application-delivery.js:155–164` – `sent`/`failed` живёт
  только в промисе; ответ 200 уходит с `mail: 'queued'`; в журнал и
  `status.json` исход не пишется; `admin-server.js:252` печатает «письмо –
  queued» для КАЖДОЙ заявки в проде (waitForMail только в тестах); в
  `admin.html:1290–1320` и `stats()` нет ни признака, ни счётчика
  недоставленных (grep failed|queued|mail – ноль). Повторов нет по замыслу
  (smtp.js:15–16). Успех вообще не фиксируется – его признак только
  отсутствие строки об ошибке.
- P2 – перезапуск процесса между ответом 200 и концом SMTP-диалога теряет
  письмо без строки в логе.
- P2 – `/api/health` проверяет hse.ru (причина, а не симптом – даёт 503
  всему здоровью), но не `.applications/` на запись, не SMTP-конфиг, не
  последний исход письма.
- P2 – ветка `failed` не покрыта тестами (`deliver()` с падающим SMTP нигде).
- P2 – compose без `logging:` – json-file без ротации; в нём навсегда
  открытый пароль админки с первого запуска (admin-server.js:1222).
- P2 – провалы формы на клиенте (429/5xx/сеть, `application-form.js:925–950`)
  не пишутся никуда; `__dpoAnalyticsEvent` нигде не вызывается, тип для
  ошибки формы в `ALLOWED_TYPES` отсутствует.
- P3 – логи прозой без request-id; префиксы разнородны (`[auth]`,
  `[schedule]`, `collect error:`, `заявка …`); метка времени только у
  `[auth]`; не-ASCII e-mail заявителя попадает в текст ошибки (K2);
  `window.onerror`/`unhandledrejection` на клиенте не ловятся (слушатель
  в index.html:391–399 – плашка сборщика); nginx превращает 502/503 на
  `/api/collect` в 204 – «аналитика лежит» видно только по пустым дням.

Минимальные соразмерные правки (≈70 строк суммарно, ноль зависимостей):
1. `store.setMail(id, { mail, error, at })` в status.json после `send`
   (по образцу `setStatus` :183–190), `list()` подмешивает `mail`,
   `stats()` считает `mailFailed`; в admin.html «писем не ушло: N» и
   строка «Письмо: не ушло – причина» в карточке. Тест: deliver() с
   SMTP-заглушкой 535 → `list()` даёт `mail: 'failed'`.
2. Лог-строка «письмо отправлено» после исхода, а не до.
3. `/api/health`: `applications:writable`, `mail:config`, `mail:lastFailure`;
   hse.ru оставить в списке, исключить из `ok`.
4. compose: `logging: { driver: json-file, options: { max-size: "10m", max-file: "5" } }`.
5. `form-error` в `ALLOWED_TYPES` + вызов из веток не-200/catch формы +
   счётчик в `buildSummary`.

В порядке: журнал пишется до письма, 200 только после `store.save`;
пароль SMTP замаскирован во всех ошибках (тест); в логах приёма нет
ФИО/телефона/почты; аналитика без ПДн; `uncaughtException` завершает
процесс со стеком и restart поднимает; заявка при лежащей админке не
маскируется в 204 (nginx:178–184), клиент показывает телефон.

## 3. Сверка apple-design на регрессии – ВЫПОЛНЕНО

Проверены шесть коммитов очереди apple-design (fa255c3, 6022ac3, c2ee37f,
843d3d6, de29160, d4895ff) и три поздних (1aa743c, d5c57fa, a39097f) в
браузере: playwright-cli, телефон 390×844 (hasTouch, pointer:coarse) и
десктоп 1440×900, localStorage чистился. Жесты подавались указателем
через Pointer Events (код проверяет только isPrimary) – для JS это
эквивалент пальца, для нативной прокрутки нет. Скрипты `t1…t8-*.js` и
снимки `m-*.png`/`d-*.png` – в scratchpad (пропадут).

Итог: из 10 пунктов 8 работают, 1 частично (системные настройки
прозрачности и контраста), 1 с оговоркой (трекинг). Поздние коммиты
ничего из apple-очереди не сломали: a39097f снял только `.about-source`,
`sheet-gesture.js` подключён на всех 26 страницах программ. Одна
настоящая поломка – в самом коммите c2ee37f (P2 ниже).

| Пункт | Статус | Доказательство |
|---|---|---|
| Модалка заявки: тянуть вниз | ✓ | тяга 120px → `translateY(120px)`, вуаль .92; возврат → `transform:''`; тяга вверх → rubber-band −46px; флик → удалена за 1 с, `overflow` снят, фокус на кнопке; прерванное закрытие – один backdrop (`js/sheet-gesture.js:106–171`, `js/application-form.js:666–677, 740–758`) |
| Мобильное меню: смахнуть вверх | ✓ | тяга 80px → `translateY(-64px)`; флик → `hidden`, URL не менялся; Escape возвращает фокус на `#mobileMenuBtn`; ловушки нет по замыслу – панель немодальная, уход фокуса закрывает (`js/nav-menu.js:113–241`) |
| Карточка преподавателя: тот же жест | ✓ | 65 кнопок; тяга/возврат/флик/Escape как у заявки, фокус возвращается на `.dpo-teacher-more` (`js/team-modal.js:198–213`) |
| Пресс-отклик CTA | ✓ | `:active` → `matrix(0.9735…)`, переход только `transform .14s`, прерываем; под reduced-motion снят (`.landing-template.html:1358–1367`, `js/smooth-ui.js:31–43`, `program.css:179`) |
| reduced-motion / -transparency / contrast | частично | RM: transition none, ручка скрыта, жест не подключён. RT/contrast: шапки каталога, программ, privacy, ratings и `.dpo-mobile-cta` – ок; **шапка лендинга и нижние панели каталога/программ – нет** (P2 ниже) |
| rem | ✓, 7 остатков | при `html{font-size:20px}` растёт всё, кроме `.dpo-format-title` 19px; остатки px: `.landing-template.html:2963–2975` (блок «Форматы»), :3136, :3156, :3507; в js/, program.css, каталоге, privacy, ratings, 404 – ноль |
| Scroll-edge шапок | ✓, но всегда | после scroll 580 → `html.dpo-scrolled`, blur(8px), `::after`-градиент; каталог/программы/privacy/ratings – то же; НО градиент рисуется и при scrollY=0 |
| Трекинг заголовков | с оговоркой | h1 68px → −0.015em, `.dpo-h2` 42px ×6 → −0.01em, `.hero h1` каталога −0.42px; но три h2 42px с инлайн-стилем, h2 40/34px, h1 программ/privacy/ratings – `normal` |
| Хаптика | ✓ (по коду) | единственный `navigator.vibrate(10)` – экран «Спасибо!», с проверкой поддержки и гейтом reduced-motion (`js/application-form.js:872–874`) |
| Полоса фильтров, privacy/ratings | ✓ | `.filters` sticky, `::after`-градиент, под contrast – линия 1px; privacy/ratings под RT – белый без blur |

### Регрессии

- **P2 – мёртвые правила системной прозрачности/контраста для нижних
  панелей** (дефект коммита c2ee37f). В каталоге `@media
  (prefers-reduced-transparency)` и `(prefers-contrast)` для `.mobile-cta`
  стоят на строках 111–118, а базовое `.mobile-cta { background:
  rgb(var(--surface)/.94); backdrop-filter: blur(12px) }` – на 356–362,
  ниже и с той же специфичностью, поэтому побеждает. То же в
  `programs/program.css`: @media на :45–50, базовый `.buy-bar` внутри
  `@media (max-width:900px)` на :196–198. Замер под RT и contrast:more:
  `.mobile-cta` = `rgba(255,255,255,.94) + blur(12px)`, рамка не темнеет.
  Зеркало ловушки 21.08 «специфичность бьёт порядок»: здесь порядок бьёт
  намерение. Лечение – перенести @media ниже базовых правил.
- **P2 – шапка лендинга не знает о `prefers-reduced-transparency` /
  `prefers-contrast`.** В `.landing-template.html` этих @media нет вовсе
  (grep – 0), сжатая `.dpo-header-bar` (:655–659) под обеими настройками
  остаётся `rgba(250,247,241,.94) + blur(8px)` без рамки; в
  `js/smooth-ui.js:224–229` покрыта только `.dpo-mobile-cta`. Коммит
  c2ee37f заявляет настройки сделанными – для главной это не так.
- P3 – семь кеглей в px (см. таблицу): при системном увеличении шрифта
  блок «Форматы» и кнопки тайлов не растут.
- P3 – на телефоне при root 20px шапка каталога переполняет экран
  (`scrollWidth 402 > 390`): «Версия для слабовидящих» и «← Главная»
  не переносятся.
- P3 – после закрытия мобильного меню жестом фокус остаётся на BODY:
  `close(trigger)` в `js/nav-menu.js:227` без `restoreFocus` (после
  Escape, :349, – с ним). На таче незаметно, при внешней клавиатуре и
  скринридере точка возврата теряется.

### Остаточные отклонения от apple-design (не из очереди)

1. **P2 – scroll-edge горит всегда** (разд. 12 скилла: только там, где
   плавающий UI перекрывает контент). `::after`-градиент под всеми
   шапками и полосой фильтров нарисован при scrollY=0; на кремовом герое
   каталога это видимая полоса тени в покое. Включать вместе с
   `html.dpo-scrolled` (лендинг :596–600) или по классу от `scrollY>0`
   на остальных (`Каталог программ.html:100–106`, `program.css:40–42`,
   privacy, ratings).
2. **P2 – трекинг по классу, а не по размеру** (разд. 15). Пять h2 34–42px
   на лендинге без трекинга рядом с шестью `.dpo-h2` 42px с −0.01em
   (:2806, :2923, :3471, :4375); h1 42px программ (`program.css:83`),
   privacy (:70), ratings (:69) – без. Одно правило по размеру закроет всё.
   Совпадает с расхождением документа №2 в разделе 4.
3. P3 – сжатие шапки анимирует `height` и `padding` (`.landing-template.html:591`,
   `transition: height .18s ease, padding .18s ease`) – перекладка всей
   страницы каждый кадр; довесок handoff 20.08, противоречит разд. 11.
   В заповеднике решений заказчика («анимация сжатия шапки») – трогать
   только с его решения.
4. P3 – порог закрытия листа от высоты окна, а не экрана
   (`js/sheet-gesture.js:150`: `max(140, offsetHeight*0.4)`; лист заявки
   1037px → порог 415px из 844 видимых) – медленная тяга на полэкрана
   возвращает окно, закрывает только флик.
5. P3 – под reduced-motion жест выключен целиком (`sheet-gesture.js:44, :51`,
   `nav-menu.js:116`): пользователь теряет и способ закрытия, и подсказку;
   уместнее оставить 1:1-тягу и закрывать кросс-фейдом (разд. 14).
6. P3 – вход модалок CSS-transition 220 мс `translateY(12px) scale(.985)`
   (`application-form.js:97–102`, `team-modal.js:27–32`, quiz.js) – не
   пружина, но симметрично и прерываемо; допустимо.

Не проверено: настоящие touch-события и конфликт с нативной прокруткой
внутри длинного листа (1037px на экране 844); реальная вибрация;
покадровая пружина; опрос `js/quiz.js` как третье окно (видимого
триггера на лендинге нет, код подключения совпадает с формой).
Для сведения: горизонтальная прокрутка каталога на 1440 (§2.4) была и
до apple-очереди (проверено на ea4c330).

## 4. DESIGN.md против эталонов awesome-design-md – ВЫПОЛНЕНО

Сравнивались `DESIGN.md` (963 строки: ядро 1–603, журнал 604–963) и
сайдкар `.impeccable/design.json` (20.08) с эталонами
`~/.claude/design-md/design-md/{ibm,mastercard,apple,linear.app}`,
структура дополнительно сверена со stripe и notion. Полный отчёт с
таблицей по девяти осям – `stage4-designmd.md` в scratchpad (пропадёт),
всё существенное ниже. Правится только документ, не дизайн.

Итог: по глубине правил наш документ сильнее всех четырёх эталонов
(Named Rules с тестами, раздел Motion с частотным гейтом, режим для
слабовидящих – ни у кого этого нет). По форме отстаёт: 37 % объёма
(40,7 из 108,7 КБ) – журнал; нет трёх разделов, которые есть у всех
четырёх (Responsive Behavior, Iteration Guide, Known Gaps); типографика
и тени прозой вместо таблиц; фронтматтер раздут (47 цветов и 30
типографических ролей против 20–22 и 13–16 у эталонов). Документ отстал
от кода в десяти местах, сайдкар – ещё сильнее.

### P1 – документ врёт коду (все с file:line, проверено grep'ом)

1. **Кегли: документ в px, код в rem** с коммита `de29160`
   (`clamp(2.25rem, 5.4vw, 4.25rem)` :2401, `0.8125rem` :1385).
   Численно совпадает при корне 16px, но документ о rem молчит.
2. **Трекинг заголовков.** Фронтматтер `DESIGN.md:73, :79` –
   `letterSpacing: normal`; код – `.dpo-h2 { letter-spacing: -0.01em }`
   (`.landing-template.html:1399`, «решение заказчика 03.09») и герой
   `-0.015em` (:2401). Сегодняшнее решение в документ не попало.
3. **Отбивка заголовка секции.** `DESIGN.md:421, :542` – `14px 0 40px`;
   код – `.dpo-h2 { margin: 14px 0 0 }` (:1400) + `.dpo-section-head
   { margin-bottom: 48px }` (:1412). Известное расхождение не закрыто.
4. **Body 17px** (`DESIGN.md:424`) – ступени нет: абзацы `1rem/1.7`
   (:2830), лид `1.0938rem/1.65/62ch` (:1405–1409); самые частые кегли
   – 15px (29 раз) и 13px (28 раз). Токены `figure-lead`, `title-lead`
   мёртвые.
5. **Карточка формата** – документ «32px / 14px / граница 0.07», код
   (:2951–2958) `gap: 10px`, граница 0.1, `padding: clamp(20px, 2vw,
   26px)`; правило `.dpo-format` (:948–955) в этом регионе перекрыто.
6. **Подъём карточки навигации** – в Shadow Vocabulary (`DESIGN.md:470`)
   и Cards (:508) `-5px` / `0 18px 42px`, в Motion (:573) и коде
   (`js/smooth-ui.js:162–163`) `-3px` / `0 12px 28px`. Документ
   противоречит сам себе.
7. **«Кривая применена одиннадцать раз»** (`DESIGN.md:573`) – в коде 34
   вхождения `cubic-bezier(.22,1,.36,1)` плюс около 30 переходов на
   обычном `ease` с длительностями .15/.16/.18s (шапка :591, :609, :635;
   тултип :1253; формы `js/application-form.js:115–190`, `js/quiz.js:79–88`).
   «Единственная кривая» неверна для UI-слоя – см. вопрос 1.
8. **Токен `hero-scrim`** (`DESIGN.md:17`) в коде 0 вхождений; действующий
   градиент `rgba(11,42,105,.72) / (8,33,83,.80) / (6,23,57,.93)` (:804–806).
9. **Контейнер.** `DESIGN.md:436` – «глобального контейнера нет»; код –
   `.dpo-container { max-width: 1440px; margin: 0 auto }` (:523), ×12.
10. **Тени вне словаря:** `.dpo-tag-dur` inset-кольцо (:1610), тултип
    `0 8px 24px .25` (:1630); заглавные – четыре трекинга вместо
    «единственной связки 0,12em», `.dpo-start-mon` 11px/0.06em (:898)
    не описан нигде.

Сайдкар `.impeccable/design.json` (20.08) хранит отменённые 16.08
вечером значения: `translateY(-5px)` (:739), появление `28px` (:786),
шаг `60ms` (:790), таймер `1800ms` (:795) – при том, что раздел
«Сверка сайдкара 17.08» в DESIGN.md утверждает, что он пересобран.

Подтвердилось без замечаний: акцент и производный тон, нейтрали,
`veil-ink-solid`, поле и ритм секций, `.dpo-split` 860px, шапка 110px
и порог 120, герой 86vh, радиусы 999/16/24, кольцо фокуса, `scale(0.97)`
на `:active`, окно формы, параметры появления.

### P2 – разделы, которые есть у всех четырёх эталонов

- **Responsive Behavior**: брейкпоинты 620/700/760/860/899/1080/1199
  (уже собраны в сайдкаре), мишени 44px, стратегия сворачивания шапки,
  сеток и `.dpo-split`, поведение героя ниже 760 (:816).
- **Known Gaps**: админка без фокуса и vi-режима, privacy с урезанным
  vi-режимом, 404 без него, две кривые motion, inset-тень тегов, второй
  тёмно-синий в шапке – сейчас рассыпано по Do/Don't и журналу.
- **Iteration Guide**: один компонент за раз, ключ `components:`,
  геометрия в inline-стилях генератора (урок 17.08), порядок
  `inject → build-landing`, что генерируется, `npm test`, сайдкар.
- **Note on Font Substitutes**: что сохранять при подмене HSE Slab →
  Source Serif 4 и HSE Sans → IBM Plex Sans.
- Таблицы вместо прозы в Typography (Token | Size | Weight | LH | LS |
  Use) и Elevation (Level | Treatment | Use).

### P3 – структура

- Главы 604–963 – в `docs/design-log.md`; в ядро перенести только
  действующие решения (Seline 01.09 как «референс и границы», «тексты
  заказчика – канон»). Do/Don't очистить от дат.
- `colors:` разделить на палитру / компонентные (`header-*`) /
  служебные (`loader-*`, `error-sink-*`).
- Раздел «Язык и копирайт» (en dash, «ё», sentence case, «Подать заявку»
  как единственная метка действия) – наш плюс, у эталонов его нет.
- Компоненты без спецификации: футер, тултип `data-tip`, теги, тайлы
  Топ-5, карточки сфер и преподавателей, ленты, cookie-баннер,
  мобильная панель, quiz, team-modal, манифест, corporate-cta, фильтры
  каталога; у кнопок и полей – `-focus`/`-active`/`-disabled`.
- Motion – отдельный H2, не подраздел Components.
- Тёмная тема не нужна (ни один эталон её не документирует); хватит
  пары `inverse-canvas`/`inverse-ink` для секций на `veil-ink-solid`.

### Вопросы владельцу (пробелы системы, не документа)

1. Вторая кривая `ease` в шапке, тултипах и формах – норма для UI-хрома
   (узаконить как `ease-ui`) или дефект, сводить к `--dpo-ease`?
2. Нужны ли токены `disabled` (сейчас только `opacity: .6` у отправки)
   и `focus-ring` как цвет?
3. `.dpo-start-mon` 11px/0.06em – признать ступень `micro-caps` или
   привести к 0.12em?
4. Сайдкар – пересобирать после каждой правки ядра или отказаться: он
   второй раз старше документа.

## 5. Контрольная impeccable critique – ВЫПОЛНЕНО: 29/36 (81 %, Good)

Двухагентная критика по методике Impeccable, как 02.09: агент A –
дизайн-ревью в браузере (playwright-cli, 1440×900 и 390×844,
localStorage чист), агент B – детектор анти-паттернов + замеры. Оценивалось
состояние `98bce29` целиком (после d5c57fa и a39097f лежат ещё 10
коммитов: овалы в карточках, портреты на синем, очередь apple-design).
Снимок в `.impeccable/critique/` НЕ создавался (read-only), полные
отчёты `stage5-critique-A.md`/`-B.md` и скриншоты `shots/` – в scratchpad.

Тренд: 21/36 → 23/40 → 28/40 → 22/36 (01.09) → 25/36 (02.09) → **29/36**.
Впервые выше границы Good (70 %). Рост на 4 балла – три подтверждённых
закрытия, не смена шкалы. **P0 нет, P1 нет.**

### 5.1 Design Health Score (агент A)

| # | Эвристика | Балл | Ключевой дефект |
|---|---|---|---|
| 1 | Видимость статуса | 3 | scroll-spy держит «Преподаватели» четыре секции подряд (документ, отзывы, corporate-cta, explore) |
| 2 | Соответствие миру | 4 | язык аудитории, честные документы, подсказки к овалам ПК/формат |
| 3 | Контроль и свобода | 3 (было 2) | плашка MAX ниже героя – бейдж 48px; остаток: на 390 карточка в герое ложится на «Смотреть программы» |
| 4 | Консистентность | 3 | четыре лексемы CTA; в контактах два одинаковых primary, второй уводит на hse.ru; один аватар у Telegram и MAX |
| 5 | Предотвращение ошибок | 3 | селект программ, «Кто подаёт», honeypot; живой проверки до отправки нет (`novalidate`, `js/application-form.js:419`) |
| 6 | Узнавание | 3 | те же 26 программ в трёх лентах; «Все 3 программы сферы →» там, где все 3 показаны; овалы формат+документ – шаг вперёд |
| 7 | Гибкость | 3 | два пути к заявке, якоря, опрос, «Все даты», жест закрытия; сравнения программ нет |
| 8 | Минимализм | 3 (было 2) | дыра «О нас» закрыта; остаток: мёртвая зона «Топ-5», 278px пустоты explore→contacts, 19 151px на телефоне |
| 9 | Исправление ошибок | 4 (было 2) | подтверждено вживую: 400 → пять адресных сообщений, фокус на первое поле, ввод сохраняется, «похоже, в нём опечатка» для e-mail, отдельные тексты для сети и 429 |
| 10 | Help | n/a | persuade-лендинг |
| | **Итого** | **29/36** | **Good (81 %)** |

Сверка с пятью пунктами прошлого прогона (25/36):
1. [P1] Плашка MAX перекрывает CTA – **закрыт**: карточка только в герое,
   ниже бейдж 48px (`js/channel-invite.js:206–229`); цены и кнопки
   форматов, Топ-5 и сфер свободны; на телефоне бейдж 702–750 не
   пересекает панель 762–832. Остаток – новый P2 №1.
2. [P1] Финал не продаёт – **закрыт**: `#final-cta` (:4508) после
   контактов, top 11 672 на 1440 / 18 758 на 390; `#explore` над
   контактами, как решено 02.09.
3. [P2] Дыра «О нас» – **закрыт** (колонки вровень на 1440, без разрыва
   на 390).
4. [P2] Показатели 3×«1 место» – не тронут, канон, не снижает балл.
5. [P3] Имена/детектор – **частично**: фолбэк `#1145AA` исправлен, имена
   – данные hse.ru, остальное осознанно.

Вердикт специфичности: сделан для этого продукта (Фемида и табличка HSE,
пергамент + navy + золото, лестница форматов, овалы с подсказками, 26
реальных программ, бланк с реквизитами). Родовые места: карточки отзывов
с кавычками и три карточки «Помогите нам стать лучше».

Приоритетные проблемы:
1. **[P2] На телефоне карточка MAX ложится на «Смотреть программы».**
   Герой на 390 – 1158px, карточка живёт, пока низ героя выше 120px
   (`js/channel-invite.js:211–215`) – полтора экрана. При scrollY=0
   карточка y 684–750, кнопка 646–696; `elementFromPoint(120, 690)` →
   `#channelInvite`; с двухстрочным слоганом (ротация `dpo.slogan.bag`)
   кнопка уходит целиком. Обход – «Программы» в нижней панели. Фикс: на
   узких экранах сворачивать в бейдж при scrollY > 0 либо считать
   «первый экран» до низа кнопок героя. (Агент B перекрытия не увидел,
   потому что мерил при свёрнутом бейдже.)
2. **[P2] Мёртвая зона после «Топ-5».** Карточки заканчиваются на разной
   высоте (150/225/245/270px), дальше ~185px пустоты, единственный выход
   в каталог – ссылка 13px `.dpo-marquee-note` (:3087) и ещё ~107px до
   конца секции: четверть секции 1223px – воздух перед самой важной
   ссылкой. Фикс: ссылку в строку со стрелками ленты кнопкой-outline.
3. **[P2] Два одинаковых primary в контактах.** «Подать заявку на
   обучение» (:4488) и «Подписаться на рассылку анонсов» (:4489) – один
   синий стиль, второй уводит на hse.ru без маркера внешней ссылки.
   Фикс: рассылку в outline с иконкой.
4. **[P2] Scroll-spy застревает на «Преподаватели»** (`js/smooth-ui.js:389–479`)
   для секций без пункта меню – четыре экрана подряд шапка врёт. 02.09
   это считалось «вопросом вкуса»; сегодня агент A поднял до P2. Фикс:
   снимать is-active, когда у текущей секции нет ссылки.
5. [P3] 278px одноцветной пустоты между «Помогите нам» и контактами
   (padding `clamp(64px, 9vw, 120px)` с обеих сторон при одном фоне,
   :4394, :4431); «Все 3 программы сферы →» под карточкой с тремя;
   одинаковый аватар Telegram/MAX; кольцо фокуса заголовка модалки
   (2px, offset 6px) по-прежнему читается рамкой инпута, крестик
   налезает на кольцо; при открытии мобильного меню фокус остаётся на
   body (совпадает с P3 раздела 3).

Красные флаги: Jordan – «Программы»/«Направления» синонимы, на телефоне
три входа с двумя именами; овалы «ПК» без расшифровки на таче. Casey –
бейдж MAX (x 326–374, низ 750) в 23px над «Подать заявку» панели
(x 199–367, верх 773) – зона большого пальца делится; сферы 5049px.
Riley – строка статуса «Проверьте отмеченные поля.» на телефоне ниже
кадра (y 1136 при 844), спасают inline-сообщения. HR – путь без
изменений, финальная полоса теперь и для него.

Мелкое: лента стартов при старте показывает первый билет обрезанным
(«7 / ТЯБРЯ»); овалы в Топ-5 и строка «Повышение квалификации · Онлайн
синхронный · Старт» в сферах – два языка для одного факта; при сжатии
шапки логотип теряет подпись «Центр ДПО»; в мобильном меню три
плавающих слоя (меню, карточка MAX, панель); отзывы без дат и числа;
ссылки футера 18px высотой.

Вопросы агента A владельцу:
1. Карточка MAX в герое на телефоне закрывает «Смотреть программы» –
   что дороже: подписка с первого экрана или второй путь к каталогу?
2. Рассылка анонсов в контактах как второй primary – ставка или
   наследие? Если ставка, ей нужен свой текст-обещание.
3. Сферы на телефоне – 26 % страницы. Свернуть на узких экранах в
   аккордеон по шести заголовкам с «N программ»?

### 5.2 Детектор и браузер (агент B)

Детектор impeccable 4.0.4 (`detect.mjs --json`, 46 анти-паттернов + дрейф
от DESIGN.md по цвету/кеглю/радиусу) прогнан полностью – зависимостей
вне `node:*` у него нет, ловушка «без node_modules недосчитывает» не
подтвердилась. Настоящее ограничение другое: в `js/*.js` детектор видит
только литералы ``styled`…` ``/``css`…` ``, а стили модалок собираются
конкатенацией строк – JS для него пуст, сверен grep'ом вручную.

Итог: 11 находок (5 warning, 6 advisory), реальных 8, ложных 3; ручная
сверка JS – ещё 9 advisory (7 реальных, 2 санкционированных). Прошлые
ложные срабатывания (`#102A57` в комментарии, marquee бегунка) больше
не поднимаются.

- Warning ×4 – layout-transition сжатия шапки: `height, padding` у
  `.dpo-header-bar` (`.landing-template.html:591`), `padding, gap` капсулы
  (:609), `height, padding, font-size` у `.dpo-nav-cta` (:635), `width,
  height` логотипа (:1210). Срабатывает раз на смену состояния, джанка
  не видно; заповедник handoff 20.08. Логотип заменим на `scale(.75)`.
- Ложные: `<img>` из CSS-комментария `program.css:224–228` (детектор
  читает комментарии); радиус 1px полосок бургера (:652); `2rem`
  инициалов портрета (:1889).
- Дрейф от DESIGN.md (реальный, дрейф ЗАПИСИ, не вёрстки): `.dpo-start-mon`
  11px/0.06em uppercase (:897) – ниже нижней ступени 13px и вне правила
  заглавных; девиз героя `clamp(1.375rem, 2.8vw, 2.375rem)` до 38px (:2405)
  и слэб-строка манифеста 19→23px (:2809) – две ступени, появившиеся
  01–02.09 и не записанные; `rgb(28,57,116)` панелей манифеста (:1062)
  санкционирован памятью 01.09, но токена в фронтматтере нет – будет
  всплывать каждый прогон.
- JS вручную: hover кнопки квиза `#0F3FA0` (`js/quiz.js:85`) нарушает
  правило производного состояния (`darken(accent, .22)` = `#1145AA`),
  там же `#E6DccA` (:89); `#FFFEFB !important` на hover карточек
  `#explore` (`js/smooth-ui.js:164`); радиус 18px у трёх модалок и
  мобильной панели (`application-form.js:100`, `quiz.js:66`,
  `team-modal.js:30`, `smooth-ui.js:195`) и 10px у полей
  (`application-form.js:143, :169, :185`) – четвёртый и пятый радиусы,
  в Shapes не описаны; шкала модалок 12/14/15/18/20px не записана
  (15px – 20+ мест); `.logo .sub` 10px/0.14em на внутренних страницах
  против 9px/0.1em на лендинге; em dash – 45 штук, все в комментариях
  кода. `#123F9E` в cookie-consent записан как `institute-blue-manual`.
  Чисто: program.css, каталог, запретные `#10306B` вне шапки/`#0F2D69`/
  `#FAB900` не найдены.

Браузер (Chromium, localStorage чист):
- Консоль: 0 сообщений на 1440 и 390 после прокрутки до низа; статика
  200; не-2xx только наши POST 400.
- Горизонтального скролла нет ни на одной из ширин 320/390/768/1024/1440.
  Высота документа: 320 – 22 033px, 390 – 19 151, 768 – 13 756,
  1024 – 12 454, 1440 – 11 862.
- Плашка MAX: до ответа на cookies отсутствует в DOM (решение владельца
  соблюдено); после «Принять» карточка 340×180 (390: 340×66 над
  панелью); после ухода героя – бейдж ровно 48×48 (1440: 1376,836;
  390: 326,702 – низ 750 < верх панели 762). Hit-тест `elementFromPoint`
  по 28 `[data-application]`, 37 ценам и 2 ссылкам панели: **ни бейдж,
  ни карточка, ни баннер cookies не перекрывают ни одной кнопки заявки
  и ни одной цены ни на одном вьюпорте** – прошлый P1 закрыт. Артефакт
  метода: `.dpo-tile-price` отдаёт растянутую ссылку тайла (:1551).
- Порядок секций одинаков на обоих вьюпортах: top → starts → «О нас»
  (без id) → why-us → formats → top5 → spheres → teachers → document →
  reviews → corporate-cta → explore → contacts → final-cta. `#explore`
  над контактами (a39097f исполнен), `#final-cta` – последняя секция
  (136px на 1440, 169 на 390) – прошлый P1 «финал страницы» закрыт.
- Самая длинная секция – `#spheres` (:3091): 1916px на 1440 (16 %),
  **5049px на 390 (26 % страницы, шесть экранов подряд)**; следом на 390
  `#formats` 1831, «О нас» 1548, `#document` 1353, `#top5` 1343.
- Tab на 1440: 11 стопов до «Подать заявку», скип-ссылка первая, кольцо
  видно на всех (двухслойное box-shadow, `:focus-visible`). На 390 –
  5 стопов, скип-ссылка первым стопом НЕ пришла; замер шёл после
  перезагрузки с восстановленной прокруткой и smooth scroll –
  неубедителен, проверить руками (`.skip-link` `position:absolute; top:0`, :14).
- Модалка: `role=dialog`, `aria-modal`, фокус на заголовке. Клиентской
  валидации НЕТ (`js/application-form.js:887` – сразу POST, `novalidate`);
  сервер 400 → через ~150 мс `aria-invalid` на 5 полях, тексты через
  `aria-describedby`, фокус на первом невалидном, `role=status`
  «Проверьте отмеченные поля.». Во время запроса submit `disabled` +
  «Отправляем…» (:167, :898). Esc возвращает фокус на триггер. При
  офлайне пустая форма даёт «нет связи с сервером» (:956) вместо
  подсказок по полям.

**Побочный эффект прогона – убрать руками.** Первая попытка теста
модалки ушла на настоящий приёмник превью-сервера: в
`.applications/2026-09.jsonl` (0600, вне git) одна запись «Тест Тестов»,
id `e2bcfdeb-…`, 08:11:46Z. SMTP на этой машине не задан – письма не
было. Файл состоит из этой одной строки; удаление из сессии заблокировано,
удалить: `rm ~/dpo-pravo-hse/.applications/2026-09.jsonl`. Урок: перед
прогоном формы с валидными данными перехватывать `/api/application` ДО
первой отправки или поднимать превью с заглушкой приёмника.

## 6. Итог всего прогона – сводная очередь

Пять этапов пройдены, всё read-only, код не менялся (`98bce29`).
Ниже – единая очередь по приоритетам, собранная из разделов 1–5, и
решения, без которых часть очереди брать нельзя. Правки – только по
решениям заказчика; порядок внутри уровня – по цене/эффекту.

### Critical – действия владельца, не код

- **C1** Зеркало sergeybuzanov.github.io отдаёт весь main: admin.html,
  серверный код, `.catalog-data.json`, план с ФИО сотрудницы (§2.0, §2.3).
  Settings → Pages → gh-pages / (root) или `gh api -X PUT
  repos/SergeyBuzanov/dpo-pravo-hse/pages -f source[branch]=gh-pages`;
  убрать `docs/superpowers/plans/2026-07-24-dpo-site-fixes.md` из main;
  до переключения – noindex.
- Удалить тестовую заявку `.applications/2026-09.jsonl` (§5.2).

### P1 – четыре, все функциональные

1. Форма теряет «Комментарий» – `js/application-form.js:806` (§1) +
   тест-контракт `collect()` ↔ `parseApplication` (§2.2). Первым.
2. Исход письма не сохраняется, владелец не видит недоставок –
   `lib/application-delivery.js:155–164`, admin (§2.5, ≈70 строк).
3. Лендинг на телефоне LCP 6,2 с из-за React-рендера в браузере –
   пререндер в статичный HTML на сборке (§2.1). Самая крупная работа.
4. [admin] ~26×7 полей редактора каталога без подписей –
   `admin.html:788` (§1).

### P2 – по областям (счёт: security 7, frontend 5, perf 3, quality 4,
observability 5, apple 2+2, критика 4, Vercel 17, DESIGN.md 10)

- **Быстрые и бесспорные (≤ 10 строк каждая):** мёртвые @media
  прозрачности/контраста для `.mobile-cta` и `.buy-bar` – перенести ниже
  базовых правил (§3); `.dockerignore` + `.applications/` (R1);
  `{ mode: 0o600 }` у `.admin-credentials.json` (R2); `compose logging:`
  с ротацией (§2.5, R4); `scroll-padding-top` под липкую шапку (§1);
  `catch {}` в `static-server.js:253/:266` → проверка `MODULE_NOT_FOUND` (§2.2);
  hover квиза `#0F3FA0` → `#1145AA` (§5.2); токен `rgb(28,57,116)` в DESIGN.md (§5.2).
- **Каталог:** `.tag[data-tip]::after` 250px уводит страницу вбок на
  320/768/1440; grid `minmax(300px)` при 280px; `<nav>` и лендмарки;
  26 кнопок `.card-apply` с несовпадающим именем (§2.4).
- **Лендинг, поведение:** «Подать заявку» открывает диалог И скроллит к
  контактам (`js/smooth-ui.js:255–281` capture-фаза, §2.4); scroll-spy
  застревает на «Преподаватели» (§5.1); шапка лендинга без правил
  reduced-transparency/contrast (§3); scroll-edge горит и в покое (§3);
  `style-hover` мёртв на 11 элементах – нужны CSS :hover (§1).
- **Лендинг, раскладка:** карточка MAX на «Смотреть программы» в герое
  390 (§5.1, нужно решение); мёртвая зона после «Топ-5» (§5.1); два
  primary в контактах (§5.1, нужно решение); трекинг заголовков по
  классу, а не по размеру – пять h2 и три h1 без него (§3, §4).
- **Perf:** телефону уходит 2x-герой 343 КБ – мобильный кроп + WebP (§2.1);
  js/*.js не минифицированы, 14 script-запросов (§2.1).
- **Security/privacy:** нет удаления заявки по запросу (R3); nginx
  access_log с IP вопреки privacy (R4); node:20-alpine EOL (R5); согласие
  на анонсы opt-out (R6, юристы); нет `consentAt` (R7) (§2.3).
- **Quality:** тесты на `lib/program-slug.js`, `lib/analytics-store.js`,
  `lib/admin-credentials.js` (§2.2); ветка `failed` доставки без теста (§2.5).
- **Observability:** `/api/health` без проверки `.applications/` на
  запись и SMTP-конфига; ошибки формы на клиенте никуда не пишутся (§2.5).
- **DESIGN.md (10 расхождений с кодом, §4):** rem, трекинг, отбивка
  section head, шкала текста, карточка формата, подъём карточки,
  «одиннадцать раз», `hero-scrim`, контейнер 1440, тени вне словаря –
  один коммит документа; плюс три раздела, которых нет (Responsive,
  Known Gaps, Iteration Guide).

### P3 – сквозные темы одним коммитом каждая

`touch-action: manipulation` + tap-highlight; `text-wrap: balance` на
заголовках; preload шрифтов; `env(safe-area-inset-*)` + `viewport-fit=cover`
(§1); семь кеглей в px в блоке «Форматы» и тайлах (§3); фокус на body
после жеста закрытия меню (§3, §5.1); радиусы 18/10px и шкала модалок –
записать в DESIGN.md (§5.2); 45 em dash в комментариях js/ (§5.2);
`git gc` (§2.2); журнал DESIGN.md в `docs/design-log.md` (§4).

### Решения владельца, без которых очередь не движется

1. Pages на gh-pages (C1) – решение 25.08 есть, не исполнено.
2. Лента стартов без кнопки «стоп» (WCAG 2.2.2, §1) – кнопка как у
   отзывов или зафиксировать отступление в DESIGN.md.
3. Карточка MAX в герое на телефоне: подписка с первого экрана или
   «Смотреть программы» (§5.1).
4. Рассылка анонсов в контактах – второй primary или outline (§5.1).
5. Сферы на телефоне (5049px, 26 % страницы) – аккордеон по шести
   заголовкам или оставить (§5.1, §5.2).
6. Согласие на анонсы opt-out → opt-in (R6) – юристы.
7. Вторая кривая `ease` в UI-хроме – узаконить как `ease-ui` или свести
   к `--dpo-ease` (§4).
8. Анимация сжатия шапки (`height/padding`, §3, §5.2) – заповедник handoff
   20.08, трогать?
9. Сайдкар `.impeccable/design.json` – пересобирать или снести (§4).
10. `.dpo-start-mon` 11px/0.06em – ступень `micro-caps` или 0.12em (§4, §5.2).

### Что этот прогон закрыл из прошлых

Оба P1 критики 02.09 (плашка MAX, финал), дыра «О нас», фолбэк
`#1145AA`; «npm test виснет» – отменено (160/160 за 22 с); прошлые
ложные срабатывания детектора не повторяются; очередь apple-design
1–8 подтверждена в браузере без регрессий от поздних коммитов.
