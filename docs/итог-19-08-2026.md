# Сайт Центра ДПО: анализ, предложения и состояние работ

19 августа 2026 г. Репозиторий `SergeyBuzanov/dpo-pravo-hse`, ветка `main`.

Документ собран после шести дизайн-разборов и содержит: разбор трёх претензий по
последним правкам, результаты сверки всех преподавателей, анализ и предложения по
дизайну, состояние работ и открытые вопросы.

---

## 1. Почему вы видели столбик, старую должность и «полный швах»

Короткий ответ: вы смотрели сайт **за четырнадцать минут до того**, как правки
туда попали.

| Событие | Время |
| --- | --- |
| Ваши снимки | 00:28 и 00:29 |
| Мой коммит `c17a730` запушен | **00:42** |
| GitHub Pages собрал его без ошибок | 00:42:47 |

Виноват здесь я, а не вы: в предыдущем отчёте я написал «выложено», хотя на тот
момент на боевом лежала только карусель, а три остальные правки я ещё дописывал.
Надо было назвать точно, что уже на сайте, а что нет.

Что на боевом **сейчас** – проверено запросом к самому сайту, не по памяти:

| Проверка | Результат |
| --- | --- |
| Карусель `dpo-top5-track` | есть |
| Стрелки листания | есть |
| Старая сетка из 15 карточек в столбик | нет |
| Должность Максимова | «Старший преподаватель департамента правового регулирования бизнеса НИУ ВШЭ» |
| Слово «заместитель» у Максимова | нет |
| Щеголькова | «Директор Центра дополнительного профессионального образования» |
| Логотип у Телеграма | есть |

Safari держит старую копию упорно – нужна перезагрузка с очисткой (⌘⇧R).

### Отдельно про блок документов

Тот «столбик» из дипломов – не случайность и не только кеш. У блока есть
настоящий дефект, который я нашёл и о котором докладывал: в разметке три из
четырёх бланков помечены атрибутом `hidden`, **но рантайм сборщика срезает
`hidden` при сборке страницы**. Поэтому между сборкой документа и запуском
`js/document-switch.js` есть окно, в котором видны все четыре бланка сразу.

Я наткнулся на то же самое, когда делал кнопку «Показать ещё»: она приезжала
видимой, хотя в разметке стояла скрытой.

**Это не починено.** Правка предложена в разделе 5.

---

## 2. Преподаватели: сверка всех 67 записей

### Что удалось проверить и что нет

В данных **67 записей о 65 людях**. Личные страницы на `hse.ru` есть у **25**.
Остальные 42 – внешние практики: юристы компаний, медиаторы, арбитры. Справочник
`teacherPages` не заполняется никаким скриптом, он ведётся вручную; в коммите
Бузанова записано: «25 адресов, каждый проверен на однофамильцев; у внешних
практиков страниц ВШЭ нет».

Значит, сверить должность с первоисточником можно **только для 25**. Для
остальных авторитетного источника не существует: их должности написал факультет
в карточке программы, и проверять их не с чем. Я пробовал искать людей поиском по
`hse.ru` – поиск отдаётся скриптом, а автоматический подбор по фамилии выдавал
чужие страницы. Публиковать такие «находки» опаснее, чем не искать вовсе:
ошибиться человеком хуже, чем оставить непроверенным.

### Найдено и исправлено: шесть устаревших должностей

Сверка 25 человек по личным страницам. Все шесть расхождений – в одну сторону,
данные отстали от жизни.

| Кто | Было на сайте | На `hse.ru` |
| --- | --- | --- |
| Максимов Дмитрий Михайлович | ст. преподаватель, **зам. руководителя департамента** | ст. преподаватель. Замом был **2020–2024** |
| Жирнова Наталья Александровна | **Заместитель руководителя** департамента | Доцент, научный сотрудник |
| Журавлев Михаил Сергеевич | Старший преподаватель | **Доцент** |
| Бондарева Евгения Андреевна | преподаватель | **Доцент** |
| Калимуллина Мадина Эмировна | Старший научный сотрудник института | **Доцент** |
| Синельникова Валентина Николаевна | Профессор | **Профессор-исследователь** |

Двоим приписывались должности, которых у них нет. Троих сайт занижал. Исправлено
и выложено.

**Где лежит исправление и почему именно там.** Поле `about` берётся не с личной
страницы человека, а с карточки на странице программы – её подтягивает
`fetch-program-descriptions.js`. Карточки программ устаревают. Правка в
`.catalog-data.json` была бы стёрта следующим обновлением каталога, поэтому карта
выверенных должностей лежит в `scripts/build-landing.js` и применяется при
сборке. У каждой записи комментарий, что было не так и как перепроверить.

### Найдено и не исправлено: три расхождения внутри данных

Проверка всех 67 записей на внутреннюю согласованность.

**Два человека хранятся дважды под разными написаниями:**

- «Максимов Дмитрий Михайлович» и «Дмитрий Максимов»
- «Будник Руслан Александрович» и «Руслан Будник»

Сборка их склеивает и берёт ту версию, которая попалась первой. Отсюда и
плавающие подписи на карточках. Для Максимова я закрыл это, положив в карту оба
написания, но сам источник путаницы остался.

**У одного человека две разные должности, и непонятно, какая действующая:**

> **Анастасия Албух**
> «Юрист департамента корпоративно-правовых отношений транспортного холдинга,
> тренер студенческих команд»
> «Юрист управления юридического сопровождения проектов и финансирования
> ООО «Сервисный центр Ф…»»

Это не опечатка, а два разных места работы. Нужен ответ факультета, какое верно.

**У одного нет фотографии:** Андреев Павел Викторович. Карточка показывает
монограмму – штатный запасной вариант, не ошибка, но и не полнота.

### Что предлагаю

1. **Свести дубли.** Одно написание на человека в данных, остальное – через карту
   синонимов при сборке. Тогда результат перестанет зависеть от порядка.
2. **Уточнить у факультета** должность Анастасии Албух и, если есть, выверенный
   список для остальных 42 внешних практиков. Пришлёте – добавлю в ту же карту.
3. **Попросить факультет поправить карточки на `hse.ru`.** Пока источник
   устаревает, наша карта будет расти. Это лечение симптома, а не причины.

---

## 3. Анализ дизайна

Шесть разборов, все числа – замеры вживую в браузере на пяти поверхностях, а не
впечатления.

### 3.1 Что бросается в глаза первым

**На телефоне первый экран не работает.** Два фиксированных окна – приглашение в
MAX и баннер cookies – закрывают **53,4 %** первого экрана. Заголовок обрывается
на середине слова, оба призыва к действию не видны. Замер при 375×812: окна
занимают 434 пикселя из 812.

**Цена и кнопка заявки на странице программы лежат в медиане на 4,6 экрана
вглубь**, максимум 11,6. Глубже четырёх экранов – 19 страниц из 26. Человек,
пришедший из поиска на страницу за 50 000 ₽, должен пролистать пять экранов,
чтобы узнать цену.

### 3.2 Типографика

**Заголовки не жирные, хотя система этого требует.** `HSESlab-Regular.woff2`
имеет `usWeightClass = 400` и не имеет таблицы `fvar`, а в `@font-face` объявлен
диапазон `400 600`. Запрос веса 600 попадает внутрь диапазона, и браузер **не
синтезирует жирный**. Замер ширины строки при кегле 100px: веса 300, 400, 500 и
600 дают ровно 1258,41px – то есть один и тот же шрифт.

На лендинге 128 элементов набраны слэбом, большинство просят 600. Все рисуются
400. Иерархия заголовков держится только на кегле, вся работа веса не происходит.

**20 уникальных кеглей**, восемь из них (12,5 / 13 / 13,5 / 14 / 14,5 / 15 / 15,5
/ 16) умещаются в 3,5 пикселя. Это не шкала, а дрейф: восемь размеров, которые
глаз не различает, но каждый требует отдельного решения при правке.

**Два элемента набраны системным шрифтом.** У `body` лендинга нет `font-family`
ни в одном из четырёх блоков `<style>`; остальные 343 элемента спасает то, что
каждый задаёт семейство сам. Два не задают: мобильная панель действий рисуется
**Times New Roman**, крестик приглашения – Arial.

**Предложение.** Решить вопрос со слэбом (три пути в разделе 6). Свести восемь
мелких кеглей к трём: 13 для подписей, 15 для основного текста, 16 для крупного.
Одно базовое правило `body { font-family }` закрывает обе утечки.

### 3.3 Палитра

**363 вхождения цвета, 84 уникальные записи** по семи поверхностям. Картина резко
неоднородная: каталог, страницы программ и служебные страницы живут прилично –
8–13 токенов и по десятку литералов. Лендинг выпадает: **256 литералов и 55
уникальных значений при 14 токенах**. Дисциплинированнее всех устроена админка,
внутренний инструмент.

Проблема не в количестве, а в дублировании ролей:

| Роль | Сколько значений | Какие |
| --- | --- | --- |
| Вторичный текст | **четыре** | `#6B6459` ×110, `#55503E` ×88, `#48423A` ×23, `#787165` ×1 |
| Бумага | две | `#F2ECE1` ×76, `#FBF9F5` ×66 |
| Волосяная линия | **шесть** | один `rgba(33,30,27, ·)` при альфе .07 / .10 / .12 / .14 / .22 / .25 |

При этом `#6B6459` – нейтрально-тёплый серый, а `#55503E` – **оливковый**. Два
разных семейства нейтралей вперемешку в одном тексте.

**Золото почти не существует.** `#F0B86E` встречается фоном **один раз**,
`#FFD982` текстом четыре. Заявленный второй акцент фактически отсутствует. Синий
при этом – 65 вхождений текстом и 13 фоном.

**Предложение.** Вторичный текст свести к двум значениям, оливковый убрать.
Волосяную линию – к одной альфе. По золоту принять решение: дать работу
(надзаголовки на синем, блок документов, цифры факультета) или убрать из токенов.

### 3.4 Ритм и сетка

**Вертикальный ритм на удивление хорош:** девять секций из тринадцати идут с
отступом 64/64. Выбиваются четыре. Основа есть, нужна дочистка, а не пересборка.
Но ритм плоский – страница читается как ровный список из тринадцати равнозначных
секций.

**Скругления – семь значений:** 999px ×152, 24px ×69, 16px ×28, 8px ×18, 50% ×7,
15px ×6, 12px ×1. Здесь два разных дефекта: `15px` против `16px` и `50%` против
`999px` – чистый дрейф; а 999 / 24 / 16 / 8 – осмысленная система, если её
объявить.

**Контейнера нет ни на одной поверхности.** При 2560px абзац в карточке
растягивается до 743px при кегле 14,5 – около 106 знаков при норме 60–75.
Показательно, что лид героя рядом держит 62 знака: меру строки в системе
понимают, просто не применили везде.

**Брейкпоинтов 14 уникальных значений.** У `ratings.html` и `privacy.html` нет ни
одного. А `max 720px` живёт внутри мёртвого семейства `.dpo-rank` – 75 строк CSS
и ноль элементов на странице.

**Предложение.** Три ступени ритма вместо плоских 64: 96 между главами, 64 между
секциями, 40 внутри секции. Скругления к четырём ступеням. Контейнер 1440px
внутри секций – одна правка чинит меру строки на всех широких экранах. 14
брейкпоинтов свести к четырём: 520 / 760 / 1080.

### 3.5 Доступность

**Режим для слабовидящих сам нарушает доступность.** Замер при реальном
`:focus-visible`: на лендинге **5 элементов из 6** и на `ratings.html` **5 из 6**
остаются без индикатора фокуса. В форме заявки сфокусированное поле неотличимо от
обычного. Причина: правило задаёт `outline: none`, а глобальное
`html.vi-mode * { box-shadow: none !important }` гасит кольцо.

Это нарушение ГОСТ Р 52872-2019 ровно в том режиме, ради соблюдения которого он
сделан. Правило, которое всё это чинит, **уже написано и работает в каталоге** –
достаточно скопировать дословно.

**Скип-ссылка не переносит фокус** ни на одной странице; на лендинге он вовсе
остаётся на самой ссылке. Для клавиатурного пользователя это значит, что обойти
шапку из восьми ссылок нельзя.

**Мишени меньше 44×44:** лендинг 84, каталог 35, страница программы 8. Худшее –
кнопка «подробнее» 76×18 в 65 карточках преподавателей.

**Латиница не размечена:** `lang="ru"` встречается 32 раза и все 32 – на `<html>`.
В названиях программ 89 латинских вхождений, включая заголовки.

**Что проходит и трогать не надо:** все 21 поле формы подписаны (11 обёрнуты в
`<label>`, 10 через `label[for]`), зум 200 % без горизонтальной прокрутки,
ориентиры на месте, один `h1`, ноль пропусков уровней заголовков.

### 3.6 Микрокопия

**Сайт обещает то, чего не делает.** За одним действием стоят четыре ярлыка:

| Ярлык | Где | Сколько |
| --- | --- | --- |
| «Записаться» | шапка, мобильная панель | ×2 |
| «Записаться на обучение» | герой | ×1 |
| «Подать заявку» | страницы программ | ×26 |

Проверено в браузере: все открывают **одну и ту же форму** «Заявка на обучение».
При этом оплаты на домене нет, заявку обрабатывает учебный офис вручную.
«Записаться» – это обещание записи, которого сайт не выполняет.

«Подать заявку» на 26 страницах программ – правильная формулировка, просто не
доведена до лендинга.

**Форма при этом написана честно.** Подтверждение не обещает записи, называет,
кто свяжется и когда: «Заявка принята. Учебный офис Центра ДПО свяжется с вами…
Обычно это занимает один рабочий день». В тексте ошибки стоит телефон – человек,
у которого форма не отправилась, не остаётся ни с чем.

**Пустое состояние каталога работает наполовину:** «По выбранному фильтру
программ не найдено» сообщает факт, но не даёт выхода, хотя кнопка сброса рядом.

**Скрытый риск.** В генераторе страниц программ живут три заглушки вида
«Описание пока не заполнено. Запустите `node scripts/fetch-program-descriptions.js`».
Сейчас они не показываются ни на одной из 26 страниц, но при добавлении новой
программы посетитель увидит команду терминала на витрине.

---

## 4. Что сделано за работу

17 коммитов в `main`, все тесты зелёные.

**SEO.** Семантическое ядро из 307 запросов, 162 подтверждены подсказками
Яндекса. 26 страниц программ получили короткие заголовки (было 22 из 26 длиннее
нормы, худший 168 знаков), описания из фактов, микроразметку `Course` и
`BreadcrumbList`. Каталог: ключи в заголовках, 26 курсов переведены со ссылок на
`hse.ru` на собственные адреса. Лендинг: `canonical` и микроразметка теперь
переживают подмену документа рантаймом. Робот без JavaScript получил 2 731 знак
вместо 780 и 26 ссылок на программы вместо нуля. **89 изображений** получили
описания и вернулись в поиск по картинкам.

**Блокер данных.** Одно нажатие «Сохранить в каталог» в админке обнуляло
описания, преподавателей, модули и обложки у всех 26 программ и публиковало
вместо них инструкции для разработчика. Проект уже решал эту задачу для
справочника фотографий – перенос распространён на содержательные поля, покрыт
шестью тестами.

**Аудит систем.** Восемь направлений, 80 подтверждённых находок из 111.

**Правки по ходу.** Логотип ВШЭ ведёт на `pravo.hse.ru`. Ротация девизов из банка
девяти фраз. Блок программ: 15 карточек вместо пяти, лентой с листанием вправо.
Должности шести преподавателей, полное название должности директора, единый
логотип у Телеграма и MAX.

---

## 5. Что предлагаю сделать дальше

По убыванию отношения пользы к риску.

| # | Работа | Риск | Нужно решение |
| --- | --- | --- | --- |
| 1 | Убрать наложение окон на первом экране телефона | низкий | **да** – это было ваше указание |
| 2 | Вернуть индикатор фокуса в режиме для слабовидящих | низкий | нет |
| 3 | Липкая полоса с ценой и кнопкой на страницах программ | низкий | нет |
| 4 | Блок документов: не полагаться на `hidden`, который срезает рантайм | низкий | нет |
| 5 | Базовое `font-family` на `body` – закрывает Times и Arial | низкий | нет |
| 6 | Ярлык «Подать заявку» вместо «Записаться» ×3 | низкий | **да** – видимый текст |
| 7 | Контейнер 1440px внутри секций | низкий | нет |
| 8 | Шкала кеглей: восемь мелких в три | низкий | нет |
| 9 | Скругления: семь значений в четыре | низкий | нет |
| 10 | Три ступени вертикального ритма | низкий | нет |
| 11 | Свод серых и альф линии | низкий | нет |
| 12 | Контраст границы поля и плейсхолдера | низкий | нет |
| 13 | Свести дубли преподавателей | низкий | нет |
| 14 | Скип-ссылка переносит фокус | средний | трогает общий скрипт |
| 15 | Мишени до 44px | средний | меняет высоту полосы фильтров |

Пункты 2–5, 7–13 можно делать сразу.

---

## 6. Открытые вопросы

**Три решения за вами:**

1. **Вес слэба.** Запросить у ВШЭ `HSESlab-SemiBold`; сузить диапазон в
   `@font-face` до `400`, чтобы браузер честно синтезировал жирный; или признать
   один вес и строить иерархию на кегле. От ответа зависит вся типографическая
   шкала.
2. **Золото.** Дать работу или убрать из токенов.
3. **Ярлык заявки.** Менять «Записаться» на «Подать заявку» или оставить.

**Два по фронтенду V2:**

4. `DESIGN.md` на 661 строку – дополняем разделом про V2 или перезаписываем.
5. Анимация: сдержанный подход (движение только на переходах) или постоянная
   микроанимация. Скиллы проекта здесь прямо противоречат друг другу.

**Два по данным:**

6. Должность Анастасии Албух – какая из двух действующая.
7. Выверенный список должностей для 42 внешних практиков, если он есть.

---

## 7. Что осталось непроверенным

Честно о границах.

- **42 преподавателя из 65** не имеют страниц на `hse.ru`, и сверить их должности
  не с чем. Автоматический поиск по фамилии выдавал чужие страницы – от него
  отказался.
- **Домен** остаётся заглушкой `example.com` в 306 местах по вашему решению.
  Меняется одной командой `node scripts/set-domain.js <домен> --apply`.
- **Фронтенд V2** не начинался. Предложение с планом – `docs/frontend-v2-proposal.md`.
- **Десять находок высокой важности** из аудита систем не чинились: заявки с
  персональными данными не удаляются никогда, портреты весят 1,39 МБ вместо 136 КБ,
  зеркало на GitHub Pages отдаёт наружу весь репозиторий.
- В рабочем дереве лежит **чужая незакоммиченная правка** `404.html` – не моя, не
  трогал.

---

## Приложение: где что лежит

| Документ | О чём |
| --- | --- |
| `docs/seo-semantic-core.md` | 307 запросов, карта «страница → ключи → заголовки» |
| `docs/frontend-v2-proposal.md` | 81 находка, план переноса лендинга в статику |
| `docs/audit-2026-08-full.md` | аудит восьми систем |
| `docs/handoff-v2.md` | токены, компоненты, состояния, брейкпоинты |
| `docs/contrast-audit.md` | 58 пар текст/фон |
| `docs/color-system-audit.md` | 824 вхождения цвета |
| `docs/hero-slogan-rotation.md` | требования по ротации девизов |
