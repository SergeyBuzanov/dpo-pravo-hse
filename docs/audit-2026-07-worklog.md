# Журнал работ по аудиту (июль 2026)

Хронологическая запись выполнения плана `superpowers/plans/2026-07-24-dpo-site-fixes.md`:
что сделано по каждой задаче, какими коммитами, что нашло ревью, какие решения
принимались по ходу и что осталось открытым.

Файл нужен, чтобы через полгода можно было ответить «почему здесь так сделано»,
не поднимая переписку: сообщения коммитов объясняют отдельную правку, а журнал —
последовательность и причины. Записи добавляются в конец, задним числом не
правятся; если решение изменилось, пишется новая запись.

Читать снизу вверх, если нужен текущий статус: последняя запись — самая свежая.

---

Task 1: complete (commits 993adb9..2c465bf, review clean; Minor: раздел README вставлен в конец файла)
Task 2: complete (commits e07a8f9..b92e440, review clean после фикса BOM; Minor отложено: отрицательная цена и цена строкой не покрыты тестами)
Task 3: complete (commits 66850f9..84d89f6, review clean)
  Minor (отложено): isoDate на дата-only ISO-строке снова дала бы сдвиг; сейчас не риск — parseInitialState всегда даёт число.
  ОТКРЫТЫЙ ХВОСТ: закоммиченный каталог всё ещё содержит старые даты со сдвигом (снимок до фикса).
    Нужен реальный прогон `node update-catalog.js` с коммитом результата ближе к концу плана (после Задачи 18).
Task 4: complete (commits 84d89f6..f6a472f, review clean)
  Minor (плановый, не чинили): require(formatDate) стоит в середине tests/unit/date.test.js, а не рядом с require(isoDate) — так написано в брифе дословно. На триаж финального ревью.
Task 5: complete (commits f6a472f..58a4a16, review clean)
  Minor (плановые, не чинили): сообщение сетевой ошибки теряет err.cause (в undici там настоящая причина: ECONNREFUSED/ENOTFOUND);
    ветка err.name==='AbortError' недостижима — AbortSignal.timeout даёт TimeoutError, внешнего AbortController у функции нет. На триаж финального ревью.
Task 6: complete (commits 58a4a16..d93d055, review clean после фикса)
  Закрыто по ревью: тест на ветку сбоя renameSync (проверен мутациями), оговорка про Windows EPERM в комментарии.
  Minor (отложено): вторая проверка в тесте «исходный файл цел» тривиальна; tmp-имя по одному process.pid не защищает от параллельных вызовов (закрывается Задачей 8);
    при падающем тесте временные каталоги dpo-* не убираются (rmSync не в finally) — общее свойство всех тестов файла.
Task 6б: complete (commits d93d055..0ee5288)
  Первое ревью: CRITICAL — замены __proto__/new Date шли по сырому тексту и портили строковые значения. Закрыто (вариант «а»: обе операции внутри посимвольного прохода) + 4 теста, проверены мутацией.
  Второе ревью: IMPORTANT — фикс внёс квадратичную сложность (out.replace на накопленной строке). Закрыто переходом на массив кусков; линейность подтверждена замером контролёра: 8000/16000/32000 → 17/32/61 мс.
  ДОЛГ ФИНАЛЬНОГО РЕВЬЮ: после последнего фикса третьего цикла ревью субагентом не было — контролёр проверил сам (27/27 юнитов, чистое дерево, независимый замер линейности и корректности разбора). Финальному ревью ветки стоит посмотреть quoteKeysOutsideStrings отдельно.

=== СТОП 2026-07-25: PR не создан ===
На origin/dev обнаружены 4 коммита (f5671da, 4564dee, 060595a, de1e769), которых нет в main.
index.html на dev = 37,9 КБ (статика) против 2715,6 КБ на main — Задачи 12-16 плана там уже сделаны.
Также на dev: Docker+CI+nginx, 404.html, логотипы ВШЭ, hero webp, чистка шрифтов, lastmod в sitemap.
На dev НЕТ: фикса цены, isoDate (дата в JSON-LD), защиты formatDate, атомарной записи, string-safe парсера, юнит-тестов.
Конфликтные файлы при слиянии: lib/hse-catalog.js, update-catalog.js, admin-server.js, tests/run.sh, tests/README.md.
Вопрос о базе PR задан владельцу, ответа нет. ОСТАТОК ПЛАНА НЕ ТРОГАТЬ до решения.
Побочно исправлено: remote.origin.fetch был ограничен только main (следствие клона --depth 1) -> приведён к +refs/heads/*:refs/remotes/origin/*

=== 2026-07-25: ветка перенесена на dev, PR #1 открыт ===
Владелец выбрал вариант «rebase поверх dev + PR в dev».
Rebase 16 коммитов на origin/dev (4000406). Конфликты разведены вручную:
  tests/run.sh — одинаковый по смыслу фикс с обеих сторон, взят вариант dev;
  lib/hse-catalog.js — таймаут с обеих сторон, взят наш (25 с + TimeoutError + тест), комментарий dev сохранён;
  update-catalog.js — объединены атомарная запись (наша) и updateSitemapLastmod (dev).
Страховка: локальная ветка backup/fix-audit-2026-07-premrebase = 0ee5288 (состояние до rebase).
Найдено автослиянием, молча и без конфликта: smoke_test.py — dev-овский test_alumni
  распаковывал collect_page в 3 значения, а наш collect_page стал возвращать 4. Тест падал.
  Починено коммитом 341cbf5 + проверка 4xx/5xx для клуба выпускников. Смоук 39/39.
Доп. фикс 0aeebd2: updateSitemapLastmod писал дату через toISOString() (UTC) — тот же сдвиг
  на сутки, что и в Задаче 3. Переведено на isoDate, 6 тестов, проверено мутацией. Юниты 33/33.
HEAD 0aeebd2, запушен (--force-with-lease). PR: https://github.com/Bogolubov-creator/dpo-pravo-hse/pull/1

ИЗМЕНЕНИЕ ОБЪЁМА ПЛАНА (обнаружено при разборе dev):
  Задачи 7 и 8 (CSRF-защита /api/update и блокировка параллельного обновления с 409) на dev УЖЕ СДЕЛАНЫ.
  Хвосты по ним для ревью: проверка Sec-Fetch-Site пропускает запрос при отсутствии заголовка,
  Origin не проверяется, server.listen не спрятан под require.main === module, img-src в CSP не проверен.
  Задачи 12-16 (главная статикой) на dev сделаны.
  ОСТАЮТСЯ: 9 (контраст --ink-mute), 10 (a11y фильтров), 11 (переименование в catalog.html),
  17 (заголовки хостинга), 18 (домен и sitemap), 19 (гигиена).
  NB: dev уже ссылается на /catalog в sitemap и в update-catalog.js — Задача 11 согласуется с этим.

Task 11: complete (ветка task-11/rename-catalog, коммит d2f8f1e, стековый PR #2 с базой fix/audit-2026-07)
  git mv «Каталог программ.html» -> catalog.html + все ссылки: index.html, admin.html, маршрут admin-server.js,
    CATALOG_FILE в update-catalog.js, ЧПУ-правила docker/nginx.conf, смоук-тест, README, tests/README,
    комментарии в sitemap.xml и docker-compose.yml. grep по старому имени и по %D0%9A...-форме пустой.
  nginx: редирект со старого кириллического пути заменён на /catalog.html -> 301 /catalog (сайт не опубликован,
    редиректить не с чего; дублирующий адрес с расширением сводится к каноническому, как у privacy).
  НОВАЯ проверка смоук-теста: все относительные ссылки главной запрашиваются, статус < 400.
    Написана до правок, красная наблюдалась, после — проверена мутацией (возврат старого href роняет ровно её).
    Мотив: «нет битых ресурсов» ловит только реально загруженное, а по ссылке браузер не ходит без клика.
  Смоук 40/40, юниты 34/34. node update-catalog.js после переименования: 20 программ, маркеры на месте,
    sitemap обновляется; прогон откачен (реальный снимок каталога — в конце плана).
  Админка проверена вручную: GET /catalog.html -> 200, старый адрес -> 404, ссылка в admin.html новая.
  НЕ ПРОВЕРЕНО: синтаксис docker/nginx.conf — docker на машине нет, CI его тоже не валидирует
    (собирается только образ админки + docker compose config). Предложен шаг nginx -t в CI, решение за владельцем.
  Ревью субагентом не проводилось (в этой сессии субагенты не используются) — на финальное ревью.

=== 2026-07-25: остаток плана, ветка fix/audit-part2, PR #3 (база task-11/rename-catalog) ===
Task 9: complete (5e857ba). На dev цвет уже сменён на #666666 — добавлен только сторож contrast_ratio
  (формула WCAG на фактическом фоне). Зелёный сразу, честность подтверждена мутацией: #6B7A99 роняет
  обе проверки с цифрами аудита (4,31 и 3,92).
Task 10: complete (525c893). aria-pressed в renderChip + разметку + обработчик, role=group с aria-label,
  role=status/aria-live=polite на #empty, type=button. 4 проверки наблюдались красными.
  Регенерация update-catalog.js даёт ту же разметку — проверено.
Task 19: complete
  п.1+3 (36ead72): escapeHtml экранирует апостроф; safeHseUrl -> null вместо подмены CATALOG_URL,
    программа с негодной ссылкой пропускается, пустой результат -> ошибка. 8 тестов (fetch подменён, без сети).
  п.2+6 (3ad269f) + require-безопасность из Задачи 7: uncaughtException -> process.exit(1);
    safeEqual на хешах sha256 (равная длина буферов). server.listen под require.main === module,
    экспорт { server, safeEqual, runUpdate }. 6 тестов, из них два — через spawn дочернего процесса.
  п.4 (141d819): Disallow /admin.html убран из robots.txt.
  п.5+7 (ac21523): --header-h пересчитывается (syncHeaderHeight при загрузке/resize/fonts.ready/setVi),
    prefers-reduced-motion в catalog.html. Красное состояние: перекрытие 17px, scroll-behavior 'smooth'.
  п.8, п.9 — уже были сделаны на dev.
Task 17: complete (e3154f8). docs/hosting-headers.md написан под реальность (в репозитории есть nginx.conf),
  README ссылается. nginx.conf: X-Frame-Options SAMEORIGIN -> DENY + CSP frame-ancestors 'none'
  (iframe на сайте нет нигде, в админке DENY был и раньше).
Task 18: ВЫПОЛНЕНА КРОМЕ ПОДСТАНОВКИ ДОМЕНА (8d477ca). Домен не выбран -> везде example.com.
  test_seo_consistency сверяет sitemap / canonical / og:url / ЧПУ-правила nginx / robots.txt.
  ЧПУ-адреса резолвятся ПО КОНФИГУ nginx (try_files), а не по наличию файла — иначе проверка ложно красная.
  Найдено и исправлено: у privacy.html не было OG-тегов кроме og:image. Добавлена проверка og:image
  (единственная ссылка в шапке, которую не видит ни браузер, ни проверка битых ресурсов).
  Мутации: подмена адреса в sitemap и битый og:image роняют ровно нужные проверки.
ОТКРЫТЫЙ ХВОСТ ЗАКРЫТ (236c39a): каталог пересобран апдейтером со всеми правками, даты на карточках
  и в JSON-LD совпадают (3 августа 2026 / 2026-08-03), lastmod сегодняшний.
tests/README приведён в соответствие (74b1e8b): было «28 проверок» и React-бандл на главной.
ИТОГО: смоук 77/77, юниты 50/50, CI на PR #3 зелёный.
ОСТАЁТСЯ ВЛАДЕЛЬЦУ: выбрать домен (замена example.com во всех *.html, sitemap.xml, robots.txt —
  сверка адресов поймает пропуск); решить про шаг nginx -t в CI; пункты из project_dpo_hse_findings.

=== 2026-07-25: РЕВЬЮ ПРОЙДЕНО, ВЕСЬ СТЕК ВЛИТ В dev ===
Ревью: 5 независимых проходов по origin/dev..fix/audit-part2 (правила проекта, баги в диффе,
  история файлов, соответствие кода комментариям, честность тестов). Подтвердились 4 находки,
  все исправлены коммитом bc2c707:
  1) ГЛАВНОЕ: 50 юнит-тестов не гонялись НИГДЕ (ни CI, ни tests/run.sh) -> добавлен job «Юнит-тесты».
  2) docker/nginx.conf не проверялся нигде (в образ не входит, монтируется при запуске)
     -> добавлен шаг nginx -t; в первом прогоне: «configuration file test is successful».
  3) РЕГРЕССИЯ: комментарий в sitemap.xml снова ссылался на «Каталог программ.html» —
     правку задачи 11 затёр мой же `git checkout -- sitemap.xml` после тестового прогона генератора.
     УРОК: после `git checkout -- <file>` проверять, не откатились ли вместе с прогоном свои правки.
  4) Хрупкий тест safeEqual (вырезал исходник по именам соседних функций) -> safeEqual.toString().
  + переписан вводящий в заблуждение комментарий в шапке lib/hse-catalog.js.
  НЕ ПРИНЯТО: удаление Disallow /admin.html из robots.txt — в admin.html стоит noindex,
    а Disallow помешал бы роботу его увидеть (адрес попал бы в выдачу без содержимого).
  К СВЕДЕНИЮ ВЛАДЕЛЬЦА: таймаут к hse.ru после переноса = 25 с (на dev было 30 с).
Мерж: PR #1 -> dev (518da3f), затем базы #2 и #3 переведены на dev вручную (GitHub сам не перевёл,
  т.к. ветки не удалялись), #2 -> dev (cbad0d4), #3 -> dev (5e88db3).
На dev после мержа: юниты 50/50, смоук 77/77, дерево чистое. Ветки fix/audit-2026-07,
  task-11/rename-catalog, fix/audit-part2 полностью влиты (0 коммитов вне dev), на origin остались.
ОСТАЛОСЬ ВЛАДЕЛЬЦУ: домен (замена example.com), пункты из project_dpo_hse_findings,
  расхождение main и dev, решение про 25 с vs 30 с.

=== 2026-07-25: разбор безопасности, PR #4 влит в dev (e90c83b) ===
Проверено живьём (не чтением кода): обход каталога 4 вектора -> все 404; битый URL -> 401/400;
  обрыв соединения во время тарпита -> сервер жив (process.exit(1) DoS не создал); кросс-доменный POST -> 403.
ИСПРАВЛЕНО (коммит c4f3028):
  1. CSRF fail-open: `sameSite && sameSite !== 'same-origin'` пропускал запрос БЕЗ Sec-Fetch-Site,
     Origin не проверялся. Дыра показана красным тестом — POST без единого заголовка реально
     запустил обновление каталога. Вынесено в isCrossSiteRequest(headers, host), fail-closed:
     X-Requested-With: dpo-admin -> принять; Sec-Fetch-Site -> по нему; Origin -> сверить с Host; иначе отказ.
     Админка шлёт X-Requested-With (admin.html:128), проверено живьём: 200 / 403 / 403.
  2. permissions: contents: read в workflow (было умолчание репо, часто contents: write).
  3. Actions запинены по SHA (checkout 11d5960a, setup-node 49933ea5, setup-uv d4b2f3b6).
  4. USER node + COPY --chown в Dockerfile. ТРЕБУЕТ ДЕЙСТВИЯ НА СЕРВЕРЕ: chown -R 1000:1000 /srv/dpo,
     иначе кнопка «Обновить» вернёт ошибку записи (видна в статусе админки). Записано в README.
Юниты 58/58 (8 новых на CSRF), смоук 77/77.
НЕ ДЕЛАЛОСЬ (по решению владельца — предложено, отложено):
  - script-src 'unsafe-inline' в CSP публичных страниц: от XSS не защищает. Вектора нет
    (данные hse.ru экранируются, тесты есть). Чинится выносом инлайн-скриптов или sha256-хешами.
  - compose монтирует весь репозиторий в админку на запись (.:/app) вкл. .git и .github/workflows.
    Сузить до реально переписываемых файлов; локально без docker не проверить.
Хорошо и менять не нужно: брутфорс (5 попыток/15 мин + тарпит 700мс + троттлинг 120/мин + timing-safe),
  учётка случайная и не в git, порты только на 127.0.0.1, /admin.html и /archive/ -> 404, noindex.
