# Что осталось сделать. Список на 10.09.2026

Составлен в конце сессии 09.09.2026. Состояние на этот момент:
`main = dbbf3f5` в обоих remote, зеркало `gh-pages = 14b12d1` выложено и
проверено на живом сайте, тесты 407/407, security 67/67.

---

## 1. Срочное: блок «Важно» показывает несвежее

Объявления перенесены с маркетплейса 09.09.2026, и **восемь из десяти
датированы мартом–маем**: 12.03, 16.03, 17.03, 19.03, 03.04, 10.04, 21.04,
22.05. Блок с надписью «Важно» и датой полугодовой давности читается как
заброшенный сайт.

Откуда взялось: на маркетплейсе это закрываемая плашка вверху страницы, у
нас – заметный блок в начале содержимого.

Три выхода, нужен выбор владельца:
1. не показывать объявления старше N дней (предлагаю 90) – одна проверка
   даты в `renderNotice`, полчаса;
2. убрать слово «Важно» и дату, оставить как «Анонс»;
3. снять блок совсем.

---

## 2. Долг по коду: шкала кеглей

Сведена только в пяти файлах (09.09.2026). Вне таблицы `DESIGN.md` осталось
**69 объявлений**:

| Где | Сколько |
|---|---|
| `index.html` (лендинг) | 39 |
| «Каталог программ.html» | 20 |
| `ratings.html` | 5 |
| `privacy.html`, `team-modal.js`, `quiz.js`, `doc-preview.js`, `crow-mascot.js` | по одному |

Из них 14 записаны в px, остальное – 18 разных `clamp()`. Закрывать так же,
одним заходом на поверхность, а не по одному значению. Лендинг владелец
судит по картинке – правки там видны, нужен показ.

---

## 3. Открытые вопросы по политике ПДн

Новая редакция выложена 09.09.2026 (`5b537c0`, `dbbf3f5`).

- **Утверждена ли редакция юридической службой НИУ ВШЭ?** Если да – пункт
  «privacy у юристов» уходит из очереди навсегда. Пометка об этом стоит
  комментарием в `privacy.html`.
- **Согласие на анонсы.** Пункт 5.2 политики прямо говорит: молчание и
  отсутствие отметки «Не присылать анонсы» согласием не считаются. Форма
  хранит поле `noAnnouncements` (отказ), рассылки пока нет. Перед первой
  рассылкой сделать opt-in, иначе разойдёмся с собственным документом.

---

## 4. Ждёт материалов или действий владельца

1. **Тексты бота** (`docs/тексты-бота-черновик.md`): вычитать девять
   ответов, решить по замечаниям к ответам 2 и 3.
2. **Четыре пробела бота**: оплата, счёт от организации, пропуск занятия,
   документ при неуспешном окончании. Проверено 09.09: ответов на них нет
   ни на одной странице маркетплейса – нужны тексты центра.
3. **Журнал вопросов к боту** – решено делать вторым шагом, не начат.
4. **Номер счётчика Метрики** – в `js/cookie-consent.js` заглушка
   `METRIKA_ID = null`, аналитика Яндекса не собирается.
5. **Удалить тестовую заявку** `.applications/2026-09.jsonl` – файл 0600,
   удаляет владелец.
6. **Боевой домен** вместо `example.com`: `npm run check-deploy` падает
   всегда, для публикации это блокер. Плюс `nginx -t` при деплое.
7. **Job деплоя в `ci.yml`** закомментирован – ждёт секретов.
8. **Настоящая обложка** программы 1129129056 – сейчас фирменная заглушка;
   если центр пришлёт кадр, замена займёт минуту.

---

## 5. Решено НЕ брать (не возвращаться без нового указания)

- **Видеоролики** («презентация курса», 5 программ, YouTube/VK/smotrim) –
  владелец 09.09: «ролики не надо».
- **Контакты программ с добавочными и телеграм-канал центра**
  `t.me/dpopravohse` – владелец 09.09: «не бери».
- Мебель маркетплейса: «О факультете права», «Сообщество», «Мы в
  социальных сетях», их блок похожих программ, образец документа (у всех
  26 программ там один и тот же файл, у нас свои сканы бланков).

---

## 6. Мелочи

- **Тест девизов не гоняется**: в этом python нет playwright
  (`tests/slogan_test.py`). В CI он есть.
- **Заголовок окна заявки на планшете стал 21px** вместо 26 при сведении
  шкалы – владельцу показано, возражений не было. Если позже не понравится,
  поднять ступень `title-lg`.
- **Репозиторий вырос до ~47 МБ** после зеркалирования 40 PDF. Расписания
  перекачиваются при каждом обновлении каталога: если содержимое файла не
  изменилось, новых объектов в истории не появляется.

---

## Что сделано 09.09.2026 (чтобы не искать)

`4ddc048` → `dbbf3f5`, десять коммитов:

- заглушка обложки у программы без картинки, вариант со знаком центра;
- мишени касания 44px на внутренних страницах (замер хит-тестом);
- шкала кеглей сведена с `DESIGN.md` в пяти файлах, единый крестик окон,
  ступень `title-lg`;
- перенос данных с маркетплейса: подтемы модулей, часы, язык, график,
  скидки, вычет, документы для приёма, преимущества, «Важно», вопросы и
  ответы, 40 PDF зеркалом в `files/`;
- автообновление подробностей вшито в `update-catalog.js`;
- бот отвечает про скидки и документы для приёма, карточка несёт часы;
- подпись «Заявку принимает учебный офис» снята;
- новая политика ПДн владельца выложена, три расхождения с сайтом
  устранены и в тексте, и в исходном `.docx`.
